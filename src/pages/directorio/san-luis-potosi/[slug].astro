---
/**
 * ╔══════════════════════════════════════════════════════════════════════════════════════════════════════════╗
 * ║                                                                                                          ║
 * ║   PÁGINA DINÁMICA - ESTACIÓN DE BOMBEROS EN SAN LUIS POTOSÍ                                             ║
 * ║   ══════════════════════════════════════════════════════════════════════════════════════════════════════║
 * ║                                                                                                          ║
 * ║   "El Corazón de México" - Encrucijada Industrial y Cultural                                            ║
 * ║                                                                                                          ║
 * ║   ENTERPRISE FEATURES:                                                                                  ║
 * ║   • Protocoles HAZMAT documentados con códigos                                                          ║
 * ║   • Cobertura BMW/GM con coordinación bilingüe                                                          ║
 * ║   • Rescate espeleológico certificado                                                                    ║
 * ║   • Schema.org multi-tipo enterprise                                                                     ║
 * ║   • Badges dinámicos por especialización                                                                 ║
 * ║                                                                                                          ║
 * ╚══════════════════════════════════════════════════════════════════════════════════════════════════════════╝
 */

import PageLayout from "@layouts/PageLayout.astro";
import {
  estacionesSanLuisPotosi,
  SAN_LUIS_POTOSI_SEO,
  ZONAS_SLP,
  PROTOCOLOS_SLP,
  FAQ_SLP,
  INDUSTRIA_AUTOMOTRIZ_SLP,
  TURISMO_SLP,
  ALERTAS_SLP,
  getEstacionesPorZona,
  getEstacionesHAZMAT,
  getAlertasActivas,
  generarSchemaEstacion,
  generarInterlinking,
  getPlantasAutomotrices
} from "@data/estaciones-san-luis-potosi";

// ─── Generación de Rutas Estáticas ───────────────────────────────────────────
export function getStaticPaths() {
  return estacionesSanLuisPotosi.map((estacion) => ({
    params: { slug: estacion.slug },
    props: { estacion }
  }));
}

// ─── Props y Datos ───────────────────────────────────────────────────────────
const { estacion } = Astro.props;

// Metadata dinámica
const nombreCorto = estacion.nombre.replace("Estación de Bomberos ", "").replace("Estación Central de Bomberos ", "");
const title = `${estacion.nombre} | Bomberos San Luis Potosí | BOMBERO.MX`;
// Truncar descripción para meta description SEO (max 155 chars)
function truncateForSEO(text: string, maxLength: number): string {
  if (!text || text.length <= maxLength) return text;
  const truncated = text.slice(0, maxLength);
  const lastSpace = truncated.lastIndexOf(' ');
  return lastSpace > maxLength * 0.7 ? truncated.slice(0, lastSpace) + '...' : truncated + '...';
}
const ubicacion = estacion.municipiosCubiertos?.[0] || "San Luis Potosí";
const seoDescription = `${truncateForSEO(estacion.descripcion || '', 120)} Tel: ${estacion.telefono}. ${ubicacion}, San Luis Potosí.`;

// Clasificadores
const esHAZMAT = estacion.servicios?.some(s => s.toLowerCase().includes("hazmat"));
const esEspeleologia = estacion.servicios?.some(s => s.toLowerCase().includes("espeleolog"));
const esSwiftWater = estacion.servicios?.some(s =>
  s.toLowerCase().includes("swift water") ||
  s.toLowerCase().includes("acuático")
);
const esBilingue = estacion.idiomas?.includes("Inglés");
const esCentral = estacion.nombre.toLowerCase().includes("central");
const esIndustrial = estacion.zona === "Zona Industrial Automotriz";
const esHuasteca = estacion.zona.includes("Huasteca");
const esAltiplano = estacion.zona === "Zona Altiplano";

// Zona info
const zonaKey = Object.keys(ZONAS_SLP).find(
  k => ZONAS_SLP[k as keyof typeof ZONAS_SLP].nombre === estacion.zona
);
const zonaInfo = zonaKey ? ZONAS_SLP[zonaKey as keyof typeof ZONAS_SLP] : null;

// Estaciones relacionadas
const estacionesZona = getEstacionesPorZona(estacion.zona).filter(e => e.slug !== estacion.slug);

// Alertas activas para la zona
const alertasZona = getAlertasActivas(estacion.zona);

// Protocolos relevantes
const protocolosRelevantes = PROTOCOLOS_SLP.filter(p => {
  if (esHAZMAT && (p.codigo.includes("HAZ") || p.codigo.includes("IND"))) return true;
  if (esEspeleologia && p.codigo.includes("ESP")) return true;
  if (esSwiftWater && p.codigo.includes("SWR")) return true;
  if (esAltiplano && p.codigo.includes("SAR")) return true;
  if (esHuasteca && (p.codigo.includes("FOR") || p.codigo.includes("SWR"))) return true;
  return false;
}).slice(0, 3);

// Plantas automotrices (si es zona industrial)
const plantasAutomotrices = esIndustrial ? getPlantasAutomotrices() : [];

// Interlinking
const { estados: estadosVecinos, productos: productosRelacionados } = generarInterlinking();

// FAQ relevantes
const faqRelevantes = FAQ_SLP.filter(faq => {
  const preguntaLower = faq.pregunta.toLowerCase();
  const categoriaLower = faq.categoria.toLowerCase();
  if (esHAZMAT && categoriaLower === "industrial") return true;
  if (esEspeleologia && categoriaLower === "rescate") return true;
  if (esHuasteca && categoriaLower === "huasteca") return true;
  if (esAltiplano && categoriaLower === "altiplano") return true;
  if (preguntaLower.includes("teléfono")) return true;
  return false;
}).slice(0, 4);

// Schema.org Enterprise
const schemaEstacion = generarSchemaEstacion(estacion);
const schemaOrg = {
  "@context": "https://schema.org",
  "@graph": [
    schemaEstacion,
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Inicio", "item": "https://bombero.mx" },
        { "@type": "ListItem", "position": 2, "name": "Directorio", "item": "https://bombero.mx/directorio" },
        { "@type": "ListItem", "position": 3, "name": "San Luis Potosí", "item": "https://bombero.mx/directorio/san-luis-potosi" },
        { "@type": "ListItem", "position": 4, "name": nombreCorto, "item": `https://bombero.mx/directorio/san-luis-potosi/${estacion.slug}` }
      ]
    },
    {
      "@type": "GovernmentService",
      "name": `Emergency Services - ${nombreCorto}`,
      "serviceType": estacion.servicios,
      "provider": {
        "@type": "GovernmentOrganization",
        "name": "H. Cuerpo de Bomberos de San Luis Potosí"
      },
      "areaServed": estacion.municipiosCubiertos?.join(", "),
      "availableChannel": {
        "@type": "ServiceChannel",
        "servicePhone": estacion.telefono,
        "availableLanguage": estacion.idiomas || ["Spanish"]
      }
    },
    ...(faqRelevantes.length > 0 ? [{
      "@type": "FAQPage",
      "mainEntity": faqRelevantes.map(faq => ({
        "@type": "Question",
        "name": faq.pregunta,
        "acceptedAnswer": { "@type": "Answer", "text": faq.respuesta }
      }))
    }] : [])
  ]
};
---

<PageLayout title={title} description={seoDescription}>
  <script type="application/ld+json" set:html={JSON.stringify(schemaOrg)} />
  <meta name="geo.region" content="MX-SLP" slot="head" />
  <meta name="geo.position" content={`${estacion.coordenadas.lat};${estacion.coordenadas.lng}`} slot="head" />

  <!-- ═══════════════════════════════════════════════════════════════════════════
       HERO SECTION ENTERPRISE
       ═══════════════════════════════════════════════════════════════════════════ -->
  <section class="slp-detail-hero" style={`--zone-color: ${zonaInfo?.color || '#7c3aed'}`}>
    <div class="slp-detail-hero__bg">
      <div class="slp-detail-hero__gradient"></div>
      <div class="slp-detail-hero__pattern"></div>
    </div>

    <div class="container">
      <!-- Breadcrumb -->
      <nav class="slp-detail-breadcrumb" aria-label="Breadcrumb">
        <ol>
          <li><a href="/">Inicio</a></li>
          <li><a href="/directorio">Directorio</a></li>
          <li><a href="/directorio/san-luis-potosi">San Luis Potosí</a></li>
          <li aria-current="page">{nombreCorto}</li>
        </ol>
      </nav>

      <!-- Alertas Contextuales -->
      {alertasZona.length > 0 && (
        <div class="slp-detail-alerts">
          {alertasZona.slice(0, 1).map(alerta => (
            <div class={`slp-detail-alert slp-detail-alert--${alerta.tipo}`}>
              <span class="slp-detail-alert__icon">{alerta.icono}</span>
              <span class="slp-detail-alert__text">{alerta.mensaje}</span>
            </div>
          ))}
        </div>
      )}

      <div class="slp-detail-hero__grid">
        <div class="slp-detail-hero__content">
          <!-- Badges Dinámicos -->
          <div class="slp-detail-badges">
            <span class="slp-detail-badge slp-detail-badge--zone">{estacion.zona}</span>
            {esCentral && <span class="slp-detail-badge slp-detail-badge--central">Estación Central</span>}
            {esHAZMAT && <span class="slp-detail-badge slp-detail-badge--hazmat">HAZMAT Certificado</span>}
            {esEspeleologia && <span class="slp-detail-badge slp-detail-badge--cave">Rescate Espeleológico</span>}
            {esSwiftWater && <span class="slp-detail-badge slp-detail-badge--water">Swift Water Rescue</span>}
            {esBilingue && <span class="slp-detail-badge slp-detail-badge--bilingual">Bilingüe EN/ES</span>}
            {esIndustrial && <span class="slp-detail-badge slp-detail-badge--industrial">Zona Industrial</span>}
          </div>

          <h1 class="slp-detail-hero__title">{estacion.nombre}</h1>

          <p class="slp-detail-hero__desc">{estacion.descripcion}</p>

          <div class="slp-detail-hero__address">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>
            </svg>
            <span>{estacion.direccion}</span>
          </div>

          <div class="slp-detail-hero__meta">
            <div class="slp-detail-hero__meta-item">
              <span class="slp-detail-hero__meta-label">Horario</span>
              <span class="slp-detail-hero__meta-value">{estacion.horario}</span>
            </div>
            {estacion.personal && (
              <div class="slp-detail-hero__meta-item">
                <span class="slp-detail-hero__meta-label">Personal</span>
                <span class="slp-detail-hero__meta-value">{estacion.personal} bomberos</span>
              </div>
            )}
            {estacion.tiempoRespuesta && (
              <div class="slp-detail-hero__meta-item">
                <span class="slp-detail-hero__meta-label">Tiempo Respuesta</span>
                <span class="slp-detail-hero__meta-value">{estacion.tiempoRespuesta}</span>
              </div>
            )}
            {estacion.idiomas && (
              <div class="slp-detail-hero__meta-item">
                <span class="slp-detail-hero__meta-label">Idiomas</span>
                <span class="slp-detail-hero__meta-value">{estacion.idiomas.join(", ")}</span>
              </div>
            )}
          </div>
        </div>

        <!-- Emergency Card Enterprise -->
        <div class="slp-detail-emergency">
          <div class="slp-detail-emergency__main">
            <span class="slp-detail-emergency__label">Emergencias</span>
            <a href="tel:911" class="slp-detail-emergency__911">911</a>
            <span class="slp-detail-emergency__note">Línea Nacional</span>
          </div>

          <div class="slp-detail-emergency__local">
            <span class="slp-detail-emergency__local-label">Línea Directa</span>
            <a href={`tel:${estacion.telefono.replace(/[^\d+]/g, '')}`} class="slp-detail-emergency__phone">
              {estacion.telefono}
            </a>
          </div>

          {esHAZMAT && (
            <div class="slp-detail-emergency__hazmat">
              <span class="slp-detail-emergency__hazmat-badge">HAZMAT</span>
              <span>Emergencias industriales 24/7</span>
            </div>
          )}

          {esBilingue && (
            <div class="slp-detail-emergency__bilingual">
              <span class="slp-detail-emergency__bilingual-flag">EN</span>
              <span>English coordination available</span>
            </div>
          )}

          <div class="slp-detail-emergency__actions">
            <a
              href={`https://www.google.com/maps/dir/?api=1&destination=${estacion.coordenadas.lat},${estacion.coordenadas.lng}`}
              target="_blank"
              rel="noopener noreferrer"
              class="slp-detail-emergency__action slp-detail-emergency__action--directions"
            >
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polygon points="3 11 22 2 13 21 11 13 3 11"/>
              </svg>
              <span>Cómo llegar</span>
            </a>
            {estacion.sitioWeb && (
              <a href={estacion.sitioWeb} target="_blank" rel="noopener noreferrer" class="slp-detail-emergency__action">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/>
                </svg>
                <span>Sitio Web</span>
              </a>
            )}
            {estacion.facebook && (
              <a href={estacion.facebook} target="_blank" rel="noopener noreferrer" class="slp-detail-emergency__action">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/>
                </svg>
                <span>Facebook</span>
              </a>
            )}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════════════
       SERVICES SECTION
       ═══════════════════════════════════════════════════════════════════════════ -->
  <section class="slp-detail-services">
    <div class="container">
      <div class="slp-detail-section-header">
        <span class="slp-detail-section-label">Capacidades</span>
        <h2>Servicios Especializados</h2>
      </div>

      <div class="slp-detail-services__grid">
        {estacion.servicios?.map((servicio, index) => {
          const esHazmatServ = servicio.toLowerCase().includes("hazmat");
          const esCaveServ = servicio.toLowerCase().includes("espeleolog");
          const esWaterServ = servicio.toLowerCase().includes("acuático") || servicio.toLowerCase().includes("swift");
          const esMedico = servicio.toLowerCase().includes("hospitalaria");
          const esForestal = servicio.toLowerCase().includes("forestal");

          return (
            <div class={`slp-detail-service ${esHazmatServ ? 'slp-detail-service--hazmat' : ''} ${esCaveServ ? 'slp-detail-service--cave' : ''} ${esWaterServ ? 'slp-detail-service--water' : ''} ${esMedico ? 'slp-detail-service--medical' : ''} ${esForestal ? 'slp-detail-service--forest' : ''}`}>
              <div class="slp-detail-service__icon">
                {esHazmatServ && "☢️"}
                {esCaveServ && "🕳️"}
                {esWaterServ && "🌊"}
                {esMedico && "🏥"}
                {esForestal && "🌲"}
                {!esHazmatServ && !esCaveServ && !esWaterServ && !esMedico && !esForestal && "🔥"}
              </div>
              <span class="slp-detail-service__name">{servicio}</span>
              <span class="slp-detail-service__number">{String(index + 1).padStart(2, '0')}</span>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════════════
       EQUIPMENT SECTION
       ═══════════════════════════════════════════════════════════════════════════ -->
  {estacion.equipamiento && estacion.equipamiento.length > 0 && (
    <section class="slp-detail-equipment">
      <div class="container">
        <div class="slp-detail-section-header">
          <span class="slp-detail-section-label">Flota y Equipo</span>
          <h2>Equipamiento</h2>
        </div>

        <div class="slp-detail-equipment__grid">
          {estacion.equipamiento.map(equipo => {
            const esVehiculo = equipo.toLowerCase().includes("autobomba") || equipo.toLowerCase().includes("unidad") || equipo.toLowerCase().includes("ambulancia");
            const esHazmatEq = equipo.toLowerCase().includes("hazmat") || equipo.toLowerCase().includes("encapsulado");
            const esCaveEq = equipo.toLowerCase().includes("espeleolog") || equipo.toLowerCase().includes("cuerda") || equipo.toLowerCase().includes("polipasto");
            const esWaterEq = equipo.toLowerCase().includes("lancha") || equipo.toLowerCase().includes("buceo") || equipo.toLowerCase().includes("pfd");

            return (
              <div class={`slp-detail-equipment__item ${esVehiculo ? 'slp-detail-equipment__item--vehicle' : ''} ${esHazmatEq ? 'slp-detail-equipment__item--hazmat' : ''} ${esCaveEq ? 'slp-detail-equipment__item--cave' : ''} ${esWaterEq ? 'slp-detail-equipment__item--water' : ''}`}>
                <span class="slp-detail-equipment__icon">
                  {esVehiculo && !esHazmatEq && "🚒"}
                  {esHazmatEq && "☢️"}
                  {esCaveEq && "🧗"}
                  {esWaterEq && "🚤"}
                  {!esVehiculo && !esHazmatEq && !esCaveEq && !esWaterEq && "🔧"}
                </span>
                <span class="slp-detail-equipment__name">{equipo}</span>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  )}

  <!-- ═══════════════════════════════════════════════════════════════════════════
       PROTOCOLOS SECTION (si hay relevantes)
       ═══════════════════════════════════════════════════════════════════════════ -->
  {protocolosRelevantes.length > 0 && (
    <section class="slp-detail-protocols">
      <div class="container">
        <div class="slp-detail-section-header">
          <span class="slp-detail-section-label">Documentación</span>
          <h2>Protocolos de Emergencia</h2>
        </div>

        <div class="slp-detail-protocols__grid">
          {protocolosRelevantes.map(proto => (
            <div class="slp-detail-protocol-card">
              <div class="slp-detail-protocol-card__header">
                <span class="slp-detail-protocol-card__code">{proto.codigo}</span>
                <span class="slp-detail-protocol-card__time">⏱️ {proto.tiempoMaximoRespuesta}</span>
              </div>
              <h3>{proto.tipo}</h3>
              <p>{proto.descripcion}</p>
              <div class="slp-detail-protocol-card__resources">
                <span>Recursos:</span>
                <ul>
                  {proto.recursosMinimos.slice(0, 3).map(r => <li>{r}</li>)}
                </ul>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- ═══════════════════════════════════════════════════════════════════════════
       PLANTAS AUTOMOTRICES (si es zona industrial)
       ═══════════════════════════════════════════════════════════════════════════ -->
  {esIndustrial && plantasAutomotrices.length > 0 && (
    <section class="slp-detail-plants">
      <div class="container">
        <div class="slp-detail-section-header">
          <span class="slp-detail-section-label">Cobertura Industrial</span>
          <h2>Plantas Automotrices</h2>
        </div>

        <div class="slp-detail-plants__grid">
          {plantasAutomotrices.map(planta => (
            <div class="slp-detail-plant-card">
              <h3>{planta.empresa}</h3>
              <p class="slp-detail-plant-card__name">{planta.nombrePlanta}</p>
              <div class="slp-detail-plant-card__stats">
                <span><strong>{planta.empleados.toLocaleString()}</strong> empleados</span>
                <span><strong>{planta.capacidadAnual}</strong></span>
              </div>
              <div class="slp-detail-plant-card__hazmat">
                <span>Riesgos HAZMAT:</span>
                <div class="slp-detail-plant-card__hazmat-tags">
                  {planta.riesgosHAZMAT.slice(0, 3).map(r => <span>{r}</span>)}
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- ═══════════════════════════════════════════════════════════════════════════
       COVERAGE AREA
       ═══════════════════════════════════════════════════════════════════════════ -->
  <section class="slp-detail-coverage">
    <div class="container">
      <div class="slp-detail-coverage__grid">
        <div class="slp-detail-coverage__info">
          <div class="slp-detail-section-header slp-detail-section-header--left">
            <span class="slp-detail-section-label">Cobertura</span>
            <h2>Área de Servicio</h2>
          </div>

          <div class="slp-detail-coverage__zone" style={`border-color: ${zonaInfo?.color}`}>
            <h3>{zonaInfo?.nombre}</h3>
            <p class="slp-detail-coverage__zone-short">{zonaInfo?.nombreCorto}</p>
            <p>{zonaInfo?.descripcion}</p>
            <div class="slp-detail-coverage__zone-meta">
              <span>Población: {zonaInfo?.poblacion?.toLocaleString()}</span>
              <span class={`slp-detail-coverage__risk slp-detail-coverage__risk--${zonaInfo?.nivelRiesgo?.toLowerCase().replace(/\s+/g, '-')}`}>
                Riesgo: {zonaInfo?.nivelRiesgo}
              </span>
            </div>
          </div>

          <div class="slp-detail-coverage__municipalities">
            <h4>Zonas Cubiertas</h4>
            <ul>
              {estacion.municipiosCubiertos?.map(m => <li>{m}</li>)}
            </ul>
          </div>
        </div>

        <div class="slp-detail-coverage__map">
          <iframe
            src={`https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d5000!2d${estacion.coordenadas.lng}!3d${estacion.coordenadas.lat}!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2z!5e0!3m2!1ses!2smx!4v1`}
            width="100%"
            height="350"
            style="border:0; border-radius: 16px;"
            allowfullscreen=""
            loading="lazy"
            title={`Mapa - ${estacion.nombre}`}
          ></iframe>
          <div class="slp-detail-map__coords">
            <span>GPS:</span>
            <code>{estacion.coordenadas.lat.toFixed(4)}, {estacion.coordenadas.lng.toFixed(4)}</code>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════════════
       CARACTERÍSTICAS ESPECIALES
       ═══════════════════════════════════════════════════════════════════════════ -->
  {estacion.caracteristicasEspeciales && estacion.caracteristicasEspeciales.length > 0 && (
    <section class="slp-detail-features">
      <div class="container">
        <div class="slp-detail-section-header">
          <span class="slp-detail-section-label">Distintivos</span>
          <h2>Características Especiales</h2>
        </div>

        <div class="slp-detail-features__grid">
          {estacion.caracteristicasEspeciales.map(c => (
            <div class="slp-detail-feature">
              <span class="slp-detail-feature__icon">
                {c.toLowerCase().includes("hazmat") && "☢️"}
                {c.toLowerCase().includes("espeleolog") && "🕳️"}
                {c.toLowerCase().includes("swift") && "🌊"}
                {c.toLowerCase().includes("sótano") && "🕳️"}
                {c.toLowerCase().includes("cascada") && "💧"}
                {c.toLowerCase().includes("bmw") && "🏭"}
                {c.toLowerCase().includes("gm") && "🏭"}
                {c.toLowerCase().includes("bilingüe") && "🌐"}
                {c.toLowerCase().includes("pueblo mágico") && "✨"}
                {c.toLowerCase().includes("central") && "🏛️"}
                {!c.toLowerCase().includes("hazmat") &&
                 !c.toLowerCase().includes("espeleolog") &&
                 !c.toLowerCase().includes("swift") &&
                 !c.toLowerCase().includes("sótano") &&
                 !c.toLowerCase().includes("cascada") &&
                 !c.toLowerCase().includes("bmw") &&
                 !c.toLowerCase().includes("gm") &&
                 !c.toLowerCase().includes("bilingüe") &&
                 !c.toLowerCase().includes("pueblo mágico") &&
                 !c.toLowerCase().includes("central") && "⭐"}
              </span>
              <span class="slp-detail-feature__text">{c}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- ═══════════════════════════════════════════════════════════════════════════
       FAQ SECTION
       ═══════════════════════════════════════════════════════════════════════════ -->
  {faqRelevantes.length > 0 && (
    <section class="slp-detail-faq">
      <div class="container">
        <div class="slp-detail-section-header">
          <span class="slp-detail-section-label">FAQ</span>
          <h2>Preguntas Frecuentes</h2>
        </div>

        <div class="slp-detail-faq__grid">
          {faqRelevantes.map((faq, index) => (
            <details class="slp-detail-faq__item" open={index === 0}>
              <summary>
                <span class="slp-detail-faq__category">{faq.categoria}</span>
                <span class="slp-detail-faq__question">{faq.pregunta}</span>
              </summary>
              <div class="slp-detail-faq__answer">
                <p>{faq.respuesta}</p>
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- ═══════════════════════════════════════════════════════════════════════════
       RELATED STATIONS
       ═══════════════════════════════════════════════════════════════════════════ -->
  <section class="slp-detail-related">
    <div class="container">
      <div class="slp-detail-section-header">
        <span class="slp-detail-section-label">{estacion.zona}</span>
        <h2>Otras Estaciones</h2>
      </div>

      {estacionesZona.length > 0 ? (
        <div class="slp-detail-related__grid">
          {estacionesZona.slice(0, 3).map(e => (
            <a href={`/directorio/san-luis-potosi/${e.slug}`} class="slp-detail-related__card">
              <div class="slp-detail-related__badges">
                {e.servicios?.some(s => s.toLowerCase().includes("hazmat")) && <span class="slp-detail-related__badge slp-detail-related__badge--hazmat">HAZMAT</span>}
                {e.idiomas?.includes("Inglés") && <span class="slp-detail-related__badge slp-detail-related__badge--en">EN</span>}
              </div>
              <h3>{e.nombre.replace("Estación de Bomberos ", "").replace("Estación Central de Bomberos ", "")}</h3>
              <p>{e.direccion}</p>
              <div class="slp-detail-related__meta">
                <span class="slp-detail-related__phone">{e.telefono}</span>
              </div>
            </a>
          ))}
        </div>
      ) : (
        <p class="slp-detail-related__empty">Única estación en {estacion.zona}</p>
      )}

      <div class="slp-detail-related__all">
        <a href="/directorio/san-luis-potosi" class="slp-detail-btn slp-detail-btn--outline">
          Ver todas las estaciones de San Luis Potosí
        </a>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════════════
       CTA SECTION
       ═══════════════════════════════════════════════════════════════════════════ -->
  <section class="slp-detail-cta">
    <div class="container">
      <div class="slp-detail-cta__content">
        <h2>
          {esHAZMAT ? "¿Necesitas Equipo HAZMAT Industrial?" : esEspeleologia ? "¿Equipo de Rescate Espeleológico?" : esSwiftWater ? "¿Equipo Swift Water Rescue?" : "¿Necesitas Equipamiento Profesional?"}
        </h2>
        <p>
          {esHAZMAT
            ? "Trajes encapsulados, detectores multigas, sistemas de descontaminación para industria automotriz"
            : esEspeleologia
            ? "Cuerdas certificadas, sistemas de anclaje, camillas técnicas para rescate vertical"
            : esSwiftWater
            ? "PFDs clase V, lanchas de rescate, throw bags y equipo para aguas rápidas"
            : "Equipo profesional para bomberos y brigadas de emergencia"}
        </p>
        <a href="/cotizar" class="slp-detail-cta__btn">
          Solicitar Cotización
        </a>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  /* ╔══════════════════════════════════════════════════════════════════════════════════════════════════════════╗
     ║ SAN LUIS POTOSÍ - PÁGINA DETALLE ENTERPRISE                                                              ║
     ╚══════════════════════════════════════════════════════════════════════════════════════════════════════════╝ */

  :root {
    --slp-primary: #7c3aed;
    --slp-primary-dark: #6d28d9;
    --slp-primary-light: #a78bfa;
    --slp-hazmat: #f59e0b;
    --slp-industrial: #dc2626;
    --slp-nature: #16a34a;
    --slp-water: #0ea5e9;
  }

  /* ═══ Hero ═══ */
  .slp-detail-hero {
    position: relative;
    padding: var(--space-14) 0 var(--space-16);
    background: linear-gradient(135deg, #1e1b4b 0%, #4c1d95 40%, var(--zone-color, #7c3aed) 100%);
    overflow: hidden;
  }

  .slp-detail-hero__bg { position: absolute; inset: 0; pointer-events: none; }
  .slp-detail-hero__gradient {
    position: absolute; inset: 0;
    background: radial-gradient(ellipse 80% 60% at 20% 30%, rgba(124, 58, 237, 0.3), transparent);
  }
  .slp-detail-hero__pattern {
    position: absolute; inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.03'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
  }

  .slp-detail-breadcrumb { margin-bottom: var(--space-6); position: relative; }
  .slp-detail-breadcrumb ol { display: flex; flex-wrap: wrap; gap: var(--space-2); list-style: none; padding: 0; margin: 0; }
  .slp-detail-breadcrumb li { display: flex; align-items: center; gap: var(--space-2); font-size: var(--text-sm); color: rgba(255,255,255,0.6); }
  .slp-detail-breadcrumb li:not(:last-child)::after { content: "/"; color: rgba(255,255,255,0.3); }
  .slp-detail-breadcrumb a { color: rgba(255,255,255,0.6); text-decoration: none; }
  .slp-detail-breadcrumb a:hover { color: white; }
  .slp-detail-breadcrumb [aria-current] { color: var(--slp-primary-light); font-weight: 600; }

  .slp-detail-alerts { margin-bottom: var(--space-6); position: relative; }
  .slp-detail-alert { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-4); border-radius: var(--radius-lg); }
  .slp-detail-alert--info { background: rgba(124,58,237,0.15); border: 1px solid rgba(124,58,237,0.3); }
  .slp-detail-alert--warning { background: rgba(245,158,11,0.15); border: 1px solid rgba(245,158,11,0.3); }
  .slp-detail-alert--danger { background: rgba(239,68,68,0.15); border: 1px solid rgba(239,68,68,0.3); }
  .slp-detail-alert__icon { font-size: var(--text-xl); }
  .slp-detail-alert__text { font-size: var(--text-sm); color: white; }

  .slp-detail-hero__grid { position: relative; display: grid; grid-template-columns: 1fr 400px; gap: var(--space-10); align-items: start; }

  .slp-detail-badges { display: flex; flex-wrap: wrap; gap: var(--space-2); margin-bottom: var(--space-4); }
  .slp-detail-badge { display: inline-flex; align-items: center; padding: var(--space-1) var(--space-3); border-radius: var(--radius-full); font-size: var(--text-xs); font-weight: 600; }
  .slp-detail-badge--zone { background: rgba(255,255,255,0.15); color: white; border: 1px solid rgba(255,255,255,0.25); }
  .slp-detail-badge--central { background: rgba(167,139,250,0.2); color: #c4b5fd; border: 1px solid rgba(167,139,250,0.4); }
  .slp-detail-badge--hazmat { background: rgba(245,158,11,0.2); color: #fcd34d; border: 1px solid rgba(245,158,11,0.4); }
  .slp-detail-badge--cave { background: rgba(107,114,128,0.2); color: #d1d5db; border: 1px solid rgba(107,114,128,0.4); }
  .slp-detail-badge--water { background: rgba(14,165,233,0.2); color: #7dd3fc; border: 1px solid rgba(14,165,233,0.4); }
  .slp-detail-badge--bilingual { background: rgba(34,211,238,0.2); color: #a5f3fc; border: 1px solid rgba(34,211,238,0.4); }
  .slp-detail-badge--industrial { background: rgba(220,38,38,0.2); color: #fca5a5; border: 1px solid rgba(220,38,38,0.4); }

  .slp-detail-hero__title { font-size: clamp(1.75rem, 3.5vw, 2.5rem); font-weight: 800; color: white; line-height: 1.2; margin: 0 0 var(--space-4); }
  .slp-detail-hero__desc { font-size: var(--text-base); color: rgba(255,255,255,0.9); line-height: 1.7; margin: 0 0 var(--space-6); }

  .slp-detail-hero__address { display: flex; align-items: flex-start; gap: var(--space-3); padding: var(--space-4); background: rgba(255,255,255,0.08); border-radius: var(--radius-lg); margin-bottom: var(--space-6); }
  .slp-detail-hero__address svg { flex-shrink: 0; color: var(--slp-primary-light); margin-top: 2px; }
  .slp-detail-hero__address span { font-size: var(--text-sm); color: white; line-height: 1.5; }

  .slp-detail-hero__meta { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-4); padding: var(--space-5); background: rgba(255,255,255,0.06); border-radius: var(--radius-xl); }
  .slp-detail-hero__meta-item { display: flex; flex-direction: column; gap: var(--space-1); }
  .slp-detail-hero__meta-label { font-size: var(--text-xs); color: rgba(255,255,255,0.5); }
  .slp-detail-hero__meta-value { font-size: var(--text-sm); font-weight: 600; color: white; }

  /* Emergency Card */
  .slp-detail-emergency { background: rgba(255,255,255,0.12); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.2); border-radius: var(--radius-2xl); padding: var(--space-6); }
  .slp-detail-emergency__main { text-align: center; padding-bottom: var(--space-5); border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: var(--space-5); }
  .slp-detail-emergency__label { display: block; font-size: var(--text-xs); color: rgba(255,255,255,0.6); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: var(--space-2); }
  .slp-detail-emergency__911 { display: block; font-size: 4rem; font-weight: 900; color: var(--color-fire-400); text-decoration: none; line-height: 1; margin-bottom: var(--space-1); text-shadow: 0 0 30px rgba(239, 68, 68, 0.5); }
  .slp-detail-emergency__note { display: block; font-size: var(--text-xs); color: rgba(255,255,255,0.6); }

  .slp-detail-emergency__local { text-align: center; padding-bottom: var(--space-4); border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: var(--space-4); }
  .slp-detail-emergency__local-label { display: block; font-size: var(--text-xs); color: rgba(255,255,255,0.6); margin-bottom: var(--space-2); }
  .slp-detail-emergency__phone { display: block; font-size: var(--text-2xl); font-weight: 700; color: var(--slp-primary-light); text-decoration: none; }

  .slp-detail-emergency__hazmat { display: flex; align-items: center; justify-content: center; gap: var(--space-2); padding: var(--space-3); background: rgba(245,158,11,0.15); border-radius: var(--radius-lg); margin-bottom: var(--space-3); }
  .slp-detail-emergency__hazmat-badge { padding: var(--space-1) var(--space-2); background: var(--slp-hazmat); border-radius: var(--radius-sm); font-size: var(--text-xs); font-weight: 700; color: white; }
  .slp-detail-emergency__hazmat span:last-child { font-size: var(--text-xs); color: rgba(255,255,255,0.8); }

  .slp-detail-emergency__bilingual { display: flex; align-items: center; justify-content: center; gap: var(--space-2); padding: var(--space-3); background: rgba(34,211,238,0.15); border-radius: var(--radius-lg); margin-bottom: var(--space-4); }
  .slp-detail-emergency__bilingual-flag { padding: var(--space-1) var(--space-2); background: var(--slp-primary); border-radius: var(--radius-sm); font-size: var(--text-xs); font-weight: 700; color: white; }
  .slp-detail-emergency__bilingual span:last-child { font-size: var(--text-xs); color: rgba(255,255,255,0.8); }

  .slp-detail-emergency__actions { display: flex; flex-direction: column; gap: var(--space-2); }
  .slp-detail-emergency__action { display: flex; align-items: center; justify-content: center; gap: var(--space-2); padding: var(--space-3); background: rgba(255,255,255,0.08); border-radius: var(--radius-lg); text-decoration: none; color: white; font-size: var(--text-sm); transition: all 0.2s; }
  .slp-detail-emergency__action:hover { background: rgba(255,255,255,0.15); }
  .slp-detail-emergency__action--directions { background: var(--slp-primary); }
  .slp-detail-emergency__action--directions:hover { background: var(--slp-primary-dark); }

  /* ═══ Section Styles ═══ */
  .slp-detail-section-header { margin-bottom: var(--space-8); }
  .slp-detail-section-header--left { text-align: left; }
  .slp-detail-section-label { display: inline-block; padding: var(--space-1) var(--space-3); background: rgba(124,58,237,0.1); border-radius: var(--radius-full); font-size: var(--text-xs); font-weight: 600; color: var(--slp-primary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: var(--space-3); }
  .slp-detail-section-header h2 { font-size: var(--text-2xl); font-weight: 800; color: var(--color-smoke-900); margin: 0; }

  /* ═══ Services ═══ */
  .slp-detail-services { padding: var(--space-16) 0; background: white; }
  .slp-detail-services__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-4); }
  .slp-detail-service { position: relative; display: flex; align-items: center; gap: var(--space-4); padding: var(--space-5); background: var(--color-smoke-50); border-radius: var(--radius-xl); border: 1px solid var(--color-smoke-200); transition: all 0.2s; }
  .slp-detail-service:hover { background: white; border-color: var(--slp-primary-light); box-shadow: 0 4px 12px rgba(124,58,237,0.1); }
  .slp-detail-service--hazmat { border-left: 3px solid var(--slp-hazmat); }
  .slp-detail-service--cave { border-left: 3px solid #6b7280; }
  .slp-detail-service--water { border-left: 3px solid var(--slp-water); }
  .slp-detail-service--medical { border-left: 3px solid #10b981; }
  .slp-detail-service--forest { border-left: 3px solid var(--slp-nature); }
  .slp-detail-service__icon { font-size: var(--text-2xl); }
  .slp-detail-service__name { font-size: var(--text-sm); font-weight: 600; color: var(--color-smoke-800); line-height: 1.4; flex: 1; }
  .slp-detail-service__number { position: absolute; top: var(--space-2); right: var(--space-3); font-size: var(--text-xs); font-weight: 700; color: var(--color-smoke-300); }

  /* ═══ Equipment ═══ */
  .slp-detail-equipment { padding: var(--space-16) 0; background: var(--color-smoke-50); }
  .slp-detail-equipment__grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--space-4); }
  .slp-detail-equipment__item { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-4); background: white; border-radius: var(--radius-lg); border: 1px solid var(--color-smoke-200); }
  .slp-detail-equipment__item--vehicle { border-left: 3px solid var(--slp-primary); }
  .slp-detail-equipment__item--hazmat { border-left: 3px solid var(--slp-hazmat); }
  .slp-detail-equipment__item--cave { border-left: 3px solid #6b7280; }
  .slp-detail-equipment__item--water { border-left: 3px solid var(--slp-water); }
  .slp-detail-equipment__icon { font-size: var(--text-xl); }
  .slp-detail-equipment__name { font-size: var(--text-sm); font-weight: 500; color: var(--color-smoke-700); line-height: 1.4; }

  /* ═══ Protocols ═══ */
  .slp-detail-protocols { padding: var(--space-16) 0; background: var(--color-smoke-900); }
  .slp-detail-protocols .slp-detail-section-label { background: rgba(124,58,237,0.2); color: var(--slp-primary-light); }
  .slp-detail-protocols .slp-detail-section-header h2 { color: white; }

  .slp-detail-protocols__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-4); }
  .slp-detail-protocol-card { padding: var(--space-5); background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: var(--radius-xl); }
  .slp-detail-protocol-card__header { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--space-3); }
  .slp-detail-protocol-card__code { font-family: monospace; font-size: var(--text-sm); font-weight: 700; padding: var(--space-1) var(--space-2); background: var(--slp-primary); color: white; border-radius: var(--radius-sm); }
  .slp-detail-protocol-card__time { font-size: var(--text-xs); color: rgba(255,255,255,0.6); }
  .slp-detail-protocol-card h3 { font-size: var(--text-base); font-weight: 700; color: white; margin: 0 0 var(--space-2); }
  .slp-detail-protocol-card p { font-size: var(--text-sm); color: rgba(255,255,255,0.7); margin: 0 0 var(--space-4); line-height: 1.5; }
  .slp-detail-protocol-card__resources { font-size: var(--text-xs); color: rgba(255,255,255,0.5); }
  .slp-detail-protocol-card__resources span { display: block; margin-bottom: var(--space-1); }
  .slp-detail-protocol-card__resources ul { list-style: none; padding: 0; margin: 0; }
  .slp-detail-protocol-card__resources li { color: rgba(255,255,255,0.7); padding-left: var(--space-3); position: relative; margin-bottom: var(--space-1); }
  .slp-detail-protocol-card__resources li::before { content: "•"; position: absolute; left: 0; color: var(--slp-primary-light); }

  /* ═══ Plants ═══ */
  .slp-detail-plants { padding: var(--space-16) 0; background: linear-gradient(180deg, #fef2f2, white); }
  .slp-detail-plants__grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-6); }
  .slp-detail-plant-card { padding: var(--space-6); background: white; border-radius: var(--radius-2xl); border: 1px solid var(--color-smoke-200); border-left: 4px solid var(--slp-industrial); }
  .slp-detail-plant-card h3 { font-size: var(--text-xl); font-weight: 800; color: var(--color-smoke-900); margin: 0 0 var(--space-2); }
  .slp-detail-plant-card__name { font-size: var(--text-sm); color: var(--color-smoke-600); margin: 0 0 var(--space-4); }
  .slp-detail-plant-card__stats { display: flex; gap: var(--space-4); font-size: var(--text-sm); color: var(--color-smoke-600); margin-bottom: var(--space-4); }
  .slp-detail-plant-card__hazmat span:first-child { display: block; font-size: var(--text-xs); font-weight: 600; color: var(--slp-hazmat); margin-bottom: var(--space-2); }
  .slp-detail-plant-card__hazmat-tags { display: flex; flex-wrap: wrap; gap: var(--space-1); }
  .slp-detail-plant-card__hazmat-tags span { font-size: var(--text-xs); padding: 2px 8px; background: rgba(245,158,11,0.1); border-radius: var(--radius-full); color: var(--color-smoke-600); }

  /* ═══ Coverage ═══ */
  .slp-detail-coverage { padding: var(--space-16) 0; background: white; }
  .slp-detail-coverage__grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-10); }
  .slp-detail-coverage__zone { padding: var(--space-5); background: var(--color-smoke-50); border-radius: var(--radius-xl); border-left: 4px solid; margin-bottom: var(--space-6); }
  .slp-detail-coverage__zone h3 { font-size: var(--text-lg); font-weight: 700; color: var(--color-smoke-900); margin: 0 0 var(--space-1); }
  .slp-detail-coverage__zone-short { font-size: var(--text-xs); color: var(--color-smoke-500); margin: 0 0 var(--space-2); }
  .slp-detail-coverage__zone p:last-of-type { font-size: var(--text-sm); color: var(--color-smoke-600); margin: 0 0 var(--space-3); line-height: 1.6; }
  .slp-detail-coverage__zone-meta { display: flex; gap: var(--space-4); font-size: var(--text-xs); color: var(--color-smoke-500); }
  .slp-detail-coverage__risk { padding: 2px 8px; border-radius: var(--radius-full); font-weight: 600; }
  .slp-detail-coverage__risk--medio { background: rgba(245,158,11,0.1); color: #f59e0b; }
  .slp-detail-coverage__risk--alto { background: rgba(239,68,68,0.1); color: #dc2626; }
  .slp-detail-coverage__risk--muy-alto { background: rgba(220,38,38,0.2); color: #b91c1c; }
  .slp-detail-coverage__risk--medio-alto { background: rgba(245,158,11,0.15); color: #d97706; }

  .slp-detail-coverage__municipalities h4 { font-size: var(--text-base); font-weight: 700; color: var(--color-smoke-800); margin: 0 0 var(--space-3); }
  .slp-detail-coverage__municipalities ul { list-style: none; padding: 0; margin: 0; display: grid; gap: var(--space-2); }
  .slp-detail-coverage__municipalities li { font-size: var(--text-sm); color: var(--color-smoke-600); padding-left: var(--space-4); position: relative; }
  .slp-detail-coverage__municipalities li::before { content: ""; position: absolute; left: 0; top: 8px; width: 6px; height: 6px; background: var(--slp-primary); border-radius: 50%; }
  .slp-detail-map__coords { display: flex; align-items: center; gap: var(--space-2); margin-top: var(--space-3); font-size: var(--text-sm); color: var(--color-smoke-500); }
  .slp-detail-map__coords code { padding: var(--space-1) var(--space-2); background: var(--color-smoke-100); border-radius: var(--radius-sm); font-family: monospace; font-size: var(--text-xs); }

  /* ═══ Features ═══ */
  .slp-detail-features { padding: var(--space-16) 0; background: var(--color-smoke-50); }
  .slp-detail-features__grid { display: flex; flex-wrap: wrap; justify-content: center; gap: var(--space-4); }
  .slp-detail-feature { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-4) var(--space-6); background: white; border-radius: var(--radius-full); border: 1px solid var(--color-smoke-200); }
  .slp-detail-feature__icon { font-size: var(--text-xl); }
  .slp-detail-feature__text { font-size: var(--text-sm); font-weight: 600; color: var(--color-smoke-700); }

  /* ═══ FAQ ═══ */
  .slp-detail-faq { padding: var(--space-16) 0; background: white; }
  .slp-detail-faq__grid { display: grid; gap: var(--space-4); max-width: 800px; margin: 0 auto; }
  .slp-detail-faq__item { padding: var(--space-5); background: var(--color-smoke-50); border-radius: var(--radius-xl); border: 1px solid var(--color-smoke-200); }
  .slp-detail-faq__item[open] { background: white; border-color: var(--slp-primary-light); }
  .slp-detail-faq__item summary { cursor: pointer; list-style: none; }
  .slp-detail-faq__item summary::-webkit-details-marker { display: none; }
  .slp-detail-faq__category { display: inline-block; font-size: var(--text-xs); font-weight: 600; padding: 2px 8px; background: var(--color-smoke-100); border-radius: var(--radius-sm); color: var(--color-smoke-500); text-transform: uppercase; margin-bottom: var(--space-2); }
  .slp-detail-faq__question { display: block; font-size: var(--text-base); font-weight: 600; color: var(--color-smoke-900); }
  .slp-detail-faq__answer { padding-top: var(--space-4); margin-top: var(--space-4); border-top: 1px solid var(--color-smoke-200); }
  .slp-detail-faq__answer p { font-size: var(--text-sm); color: var(--color-smoke-700); line-height: 1.7; margin: 0; }

  /* ═══ Related ═══ */
  .slp-detail-related { padding: var(--space-16) 0; background: var(--color-smoke-50); }
  .slp-detail-related__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-6); margin-bottom: var(--space-8); }
  .slp-detail-related__card { display: block; padding: var(--space-6); background: white; border-radius: var(--radius-xl); text-decoration: none; border: 1px solid var(--color-smoke-200); transition: all 0.2s; position: relative; }
  .slp-detail-related__card:hover { border-color: var(--slp-primary); box-shadow: 0 8px 24px rgba(124,58,237,0.1); transform: translateY(-2px); }
  .slp-detail-related__badges { position: absolute; top: var(--space-3); right: var(--space-3); display: flex; gap: var(--space-1); }
  .slp-detail-related__badge { display: inline-flex; align-items: center; justify-content: center; padding: 2px 8px; border-radius: var(--radius-sm); font-size: 10px; font-weight: 700; }
  .slp-detail-related__badge--hazmat { background: var(--slp-hazmat); color: white; }
  .slp-detail-related__badge--en { background: var(--slp-primary); color: white; }
  .slp-detail-related__card h3 { font-size: var(--text-lg); font-weight: 700; color: var(--color-smoke-900); margin: 0 0 var(--space-2); padding-right: var(--space-12); }
  .slp-detail-related__card p { font-size: var(--text-sm); color: var(--color-smoke-500); margin: 0 0 var(--space-4); line-height: 1.5; }
  .slp-detail-related__phone { font-size: var(--text-sm); font-weight: 600; color: var(--slp-primary); }
  .slp-detail-related__empty { text-align: center; padding: var(--space-8); color: var(--color-smoke-500); }
  .slp-detail-related__all { text-align: center; }

  .slp-detail-btn { display: inline-flex; padding: var(--space-3) var(--space-6); border-radius: var(--radius-lg); font-size: var(--text-sm); font-weight: 600; text-decoration: none; transition: all 0.2s; }
  .slp-detail-btn--outline { background: white; border: 2px solid var(--slp-primary); color: var(--slp-primary); }
  .slp-detail-btn--outline:hover { background: var(--slp-primary); color: white; }

  /* ═══ CTA ═══ */
  .slp-detail-cta { padding: var(--space-16) 0; background: linear-gradient(135deg, #1e1b4b 0%, #4c1d95 50%, var(--slp-primary) 100%); }
  .slp-detail-cta__content { text-align: center; max-width: 600px; margin: 0 auto; }
  .slp-detail-cta__content h2 { font-size: var(--text-2xl); font-weight: 800; color: white; margin: 0 0 var(--space-4); }
  .slp-detail-cta__content p { font-size: var(--text-base); color: rgba(255,255,255,0.8); margin: 0 0 var(--space-8); }
  .slp-detail-cta__btn { display: inline-flex; padding: var(--space-4) var(--space-8); background: white; border-radius: var(--radius-lg); font-size: var(--text-base); font-weight: 700; color: var(--slp-primary-dark); text-decoration: none; transition: all 0.2s; }
  .slp-detail-cta__btn:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.2); }

  /* ═══ Responsive ═══ */
  @media (max-width: 1200px) {
    .slp-detail-services__grid { grid-template-columns: repeat(2, 1fr); }
    .slp-detail-equipment__grid { grid-template-columns: repeat(3, 1fr); }
    .slp-detail-protocols__grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 1024px) {
    .slp-detail-hero__grid { grid-template-columns: 1fr; }
    .slp-detail-emergency { max-width: 400px; }
    .slp-detail-coverage__grid { grid-template-columns: 1fr; }
    .slp-detail-related__grid { grid-template-columns: repeat(2, 1fr); }
    .slp-detail-plants__grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 768px) {
    .slp-detail-hero { padding: var(--space-10) 0 var(--space-12); }
    .slp-detail-services__grid, .slp-detail-equipment__grid, .slp-detail-protocols__grid, .slp-detail-related__grid { grid-template-columns: 1fr; }
    .slp-detail-hero__meta { grid-template-columns: 1fr; }
  }
</style>
