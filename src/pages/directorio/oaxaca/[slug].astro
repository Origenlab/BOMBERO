---
/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * PÁGINA DINÁMICA - ESTACIÓN DE BOMBEROS OAXACA
 * ═══════════════════════════════════════════════════════════════════════════════
 * Página individual para cada estación de bomberos en Oaxaca
 * Con soporte bilingüe para zonas turísticas UNESCO
 * ═══════════════════════════════════════════════════════════════════════════════
 */

import PageLayout from "@layouts/PageLayout.astro";
import {
  estacionesOaxaca,
  OAXACA_SEO,
  ZONAS_OAXACA,
  CONTEXTO_OAXACA,
  INFRAESTRUCTURA_OAXACA,
  FAQ_OAXACA,
  generarSchemaEstacion,
  generarInterlinking,
  getAlertasActivas
} from "@data/estaciones-oaxaca";

// Generar rutas estáticas
export function getStaticPaths() {
  return estacionesOaxaca.map((estacion) => ({
    params: { slug: estacion.slug },
    props: { estacion },
  }));
}

const { estacion } = Astro.props;

// Meta
const title = `${estacion.nombre} | Bomberos Oaxaca | BOMBERO.MX`;
const description = `${estacion.nombre}: ${estacion.direccion}. Tel: ${estacion.telefono}. ${estacion.descripcion || `Servicios de emergencia en ${estacion.municipiosCubiertos?.join(", ")}.`}`;

// Schema.org
const schemaEstacion = generarSchemaEstacion(estacion);
const schemaBreadcrumb = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Inicio", "item": "https://bombero.mx" },
    { "@type": "ListItem", "position": 2, "name": "Directorio", "item": "https://bombero.mx/directorio" },
    { "@type": "ListItem", "position": 3, "name": "Oaxaca", "item": "https://bombero.mx/directorio/oaxaca" },
    { "@type": "ListItem", "position": 4, "name": estacion.nombre, "item": `https://bombero.mx/directorio/oaxaca/${estacion.slug}` }
  ]
};

// Interlinking
const { estados: estadosVecinos, productos: productosRelacionados } = generarInterlinking();

// Alertas para la zona de la estación
const alertasZona = getAlertasActivas(estacion.zona);

// Otras estaciones en la misma zona
const estacionesMismaZona = estacionesOaxaca
  .filter(e => e.zona === estacion.zona && e.slug !== estacion.slug)
  .slice(0, 3);

// Características especiales
const esUNESCO = estacion.caracteristicasEspeciales?.some(c =>
  c.toLowerCase().includes("unesco") ||
  c.toLowerCase().includes("monte albán") ||
  c.toLowerCase().includes("patrimonio") ||
  c.toLowerCase().includes("centro histórico")
);
const esHAZMAT = estacion.caracteristicasEspeciales?.some(c =>
  c.toLowerCase().includes("hazmat")
);
const esRescateAcuatico = estacion.caracteristicasEspeciales?.some(c =>
  c.toLowerCase().includes("acuático") ||
  c.toLowerCase().includes("surf") ||
  c.toLowerCase().includes("bahía")
);
const esCostera = estacion.zona === "Costa Oaxaqueña";
const esIstmo = estacion.zona === "Istmo de Tehuantepec";
const esAeropuerto = estacion.slug.includes("aeropuerto");
const esForestal = estacion.servicios?.some(s => s.toLowerCase().includes("forestal"));

// Determinar color de zona
const zonaKey = Object.keys(ZONAS_OAXACA).find(
  k => ZONAS_OAXACA[k as keyof typeof ZONAS_OAXACA].nombre === estacion.zona
);
const zonaColor = zonaKey ? ZONAS_OAXACA[zonaKey as keyof typeof ZONAS_OAXACA].color : "#7c3aed";

// Temporada de huracanes
const mesActual = new Date().getMonth();
const temporadaHuracanes = mesActual >= 4 && mesActual <= 10;
const temporadaSeca = mesActual >= 1 && mesActual <= 4;
---

<PageLayout title={title} description={description}>
  <!-- Schema.org -->
  <script type="application/ld+json" set:html={JSON.stringify(schemaEstacion)} />
  <script type="application/ld+json" set:html={JSON.stringify(schemaBreadcrumb)} />

  <!-- Hero Section -->
  <section class="est-hero" style={`--zone-color: ${zonaColor}`}>
    <div class="est-hero__bg">
      <div class="est-hero__gradient"></div>
    </div>
    <div class="container">
      <!-- Breadcrumb -->
      <nav class="est-breadcrumb" aria-label="Breadcrumb">
        <ol>
          <li><a href="/">Inicio</a></li>
          <li><a href="/directorio">Directorio</a></li>
          <li><a href="/directorio/oaxaca">Oaxaca</a></li>
          <li aria-current="page">{estacion.nombre.replace("Estación de Bomberos ", "").replace("Estación Central ", "")}</li>
        </ol>
      </nav>

      <!-- Alertas de Zona -->
      {alertasZona.length > 0 && (
        <div class="est-alerts">
          {alertasZona.map(alerta => (
            <div class={`est-alert est-alert--${alerta.tipo}`}>
              <span class="est-alert__icon">{alerta.icono}</span>
              <span>{alerta.mensaje}</span>
            </div>
          ))}
        </div>
      )}

      <div class="est-hero__grid">
        <div class="est-hero__content">
          <!-- Badges -->
          <div class="est-hero__badges">
            <span class="est-hero__badge est-hero__badge--zone">{estacion.zona}</span>
            {esUNESCO && (
              <span class="est-hero__badge est-hero__badge--unesco">Cobertura UNESCO</span>
            )}
            {esHAZMAT && (
              <span class="est-hero__badge est-hero__badge--hazmat">HAZMAT</span>
            )}
            {esRescateAcuatico && (
              <span class="est-hero__badge est-hero__badge--aquatic">Rescate Acuático</span>
            )}
            {esAeropuerto && (
              <span class="est-hero__badge est-hero__badge--airport">SSEI Aeronáutico</span>
            )}
            {esForestal && (
              <span class="est-hero__badge est-hero__badge--forest">Incendios Forestales</span>
            )}
          </div>

          <h1 class="est-hero__title">{estacion.nombre}</h1>

          <address class="est-hero__address">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
              <circle cx="12" cy="10" r="3"/>
            </svg>
            {estacion.direccion}
          </address>

          {estacion.descripcion && (
            <p class="est-hero__desc">{estacion.descripcion}</p>
          )}

          <!-- Quick Stats -->
          <div class="est-hero__stats">
            {estacion.personal && (
              <div class="est-hero__stat">
                <span class="est-hero__stat-value">{estacion.personal}</span>
                <span class="est-hero__stat-label">Bomberos</span>
              </div>
            )}
            {estacion.tiempoRespuesta && (
              <div class="est-hero__stat">
                <span class="est-hero__stat-value">{estacion.tiempoRespuesta.split(' ')[0]}</span>
                <span class="est-hero__stat-label">Tiempo Respuesta</span>
              </div>
            )}
            <div class="est-hero__stat">
              <span class="est-hero__stat-value">{estacion.servicios.length}</span>
              <span class="est-hero__stat-label">Servicios</span>
            </div>
          </div>
        </div>

        <!-- Emergency Card -->
        <div class="est-hero__emergency">
          <div class="est-emergency-card">
            <div class="est-emergency-card__header">Emergencias</div>
            <a href="tel:911" class="est-emergency-card__main">911</a>
            <div class="est-emergency-card__local">
              <span>Línea directa:</span>
              <a href={`tel:${estacion.telefono.replace(/[^0-9+]/g, '')}`}>{estacion.telefono}</a>
            </div>
            {estacion.telefonoEmergencias && estacion.telefonoEmergencias !== "911" && (
              <div class="est-emergency-card__local">
                <span>Emergencias:</span>
                <a href={`tel:${estacion.telefonoEmergencias.replace(/[^0-9+]/g, '')}`}>{estacion.telefonoEmergencias}</a>
              </div>
            )}
            <div class="est-emergency-card__hours">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <path d="M12 6v6l4 2"/>
              </svg>
              {estacion.horario}
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Bilingual Tourist Info (for UNESCO and coastal stations) -->
  {(esUNESCO || esCostera) && (
    <section class="est-bilingual">
      <div class="container">
        <div class="est-bilingual__card">
          <div class="est-bilingual__header">
            <span class="est-bilingual__flag">EN</span>
            <span class="est-bilingual__title">Tourist Information / Información Turística</span>
          </div>
          <div class="est-bilingual__content">
            {esUNESCO && (
              <p>
                <strong>UNESCO Heritage Protection:</strong> This fire station provides emergency coverage for
                Oaxaca's UNESCO World Heritage sites, including the Historic Centre and Monte Albán archaeological zone.
                In case of emergency, call <strong>911</strong> or the direct line: <strong>{estacion.telefono}</strong>.
              </p>
            )}
            {esCostera && (
              <p>
                <strong>Beach & Water Rescue:</strong> This station provides aquatic rescue services for the Oaxacan Coast.
                Our personnel are trained in ocean rescue and can assist in emergencies at local beaches.
                For emergencies, call <strong>911</strong> or <strong>{estacion.telefono}</strong>.
              </p>
            )}
            {estacion.slug.includes("huatulco") && (
              <p>
                Huatulco has 9 protected bays with beaches. Our bilingual staff can assist international tourists.
                <strong>US Consulate Emergency (Oaxaca):</strong> (951) 514-3054
              </p>
            )}
            {estacion.slug.includes("puerto-escondido") && (
              <p>
                Puerto Escondido is famous for Zicatela Beach (Mexican Pipeline). High waves and strong currents
                require experienced swimmers. Our rescue team is trained for surf zone emergencies.
              </p>
            )}
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- HAZMAT Industrial Info -->
  {esHAZMAT && (
    <section class="est-hazmat">
      <div class="container">
        <div class="est-hazmat__card">
          <div class="est-hazmat__header">
            <span class="est-hazmat__icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
                <line x1="12" y1="9" x2="12" y2="13"/>
                <line x1="12" y1="17" x2="12.01" y2="17"/>
              </svg>
            </span>
            <h2>Capacidad HAZMAT - Materiales Peligrosos</h2>
          </div>
          <div class="est-hazmat__content">
            <p>
              Esta estación cuenta con <strong>capacidad de respuesta HAZMAT Nivel III</strong> para atención de
              emergencias con materiales peligrosos en la zona industrial del Istmo de Tehuantepec.
            </p>
            {estacion.slug.includes("salina-cruz") && (
              <>
                <div class="est-hazmat__facility">
                  <h3>Refinería Antonio Dovalí Jaime</h3>
                  <ul>
                    <li>Capacidad: {INFRAESTRUCTURA_OAXACA.refineria.capacidad}</li>
                    <li>Tipo de riesgo: {INFRAESTRUCTURA_OAXACA.refineria.tipoRiesgo}</li>
                  </ul>
                </div>
                <div class="est-hazmat__facility">
                  <h3>Puerto de Salina Cruz</h3>
                  <ul>
                    <li>Tipo: {INFRAESTRUCTURA_OAXACA.puerto.tipo}</li>
                    <li>Tráfico: {INFRAESTRUCTURA_OAXACA.puerto.trafico}</li>
                  </ul>
                </div>
              </>
            )}
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- UNESCO Heritage Info -->
  {esUNESCO && (
    <section class="est-unesco">
      <div class="container">
        <div class="est-unesco__card">
          <div class="est-unesco__header">
            <span class="est-unesco__badge">Patrimonio Mundial UNESCO</span>
            <h2>Protección de Sitios Patrimoniales</h2>
          </div>
          <div class="est-unesco__content">
            <p>
              Esta estación es responsable de la protección de emergencia de sitios declarados
              <strong>Patrimonio de la Humanidad por la UNESCO</strong>.
            </p>
            <div class="est-unesco__sites">
              {CONTEXTO_OAXACA.patrimonioUnesco.map(sitio => (
                <div class="est-unesco__site">
                  <span class="est-unesco__site-year">Declarado: {sitio.declaracion}</span>
                  <h3>{sitio.sitio}</h3>
                  <p>{sitio.descripcion}</p>
                  <span class="est-unesco__site-visitors">{sitio.visitantesAnuales} visitantes/año</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- Seasonal Alert: Hurricane Season (Coastal) -->
  {esCostera && temporadaHuracanes && (
    <section class="est-seasonal est-seasonal--hurricane">
      <div class="container">
        <div class="est-seasonal__card">
          <div class="est-seasonal__icon">🌀</div>
          <div class="est-seasonal__content">
            <h2>Temporada de Huracanes Activa</h2>
            <p>
              La Costa Oaxaqueña se encuentra en <strong>temporada de huracanes</strong> (mayo-noviembre).
              Mantente informado sobre alertas meteorológicas y sigue las instrucciones de las autoridades.
            </p>
            <ul>
              <li>Monitorea los avisos del SMN (Servicio Meteorológico Nacional)</li>
              <li>Conoce las rutas de evacuación de tu zona</li>
              <li>Prepara un kit de emergencia con documentos importantes</li>
              <li>En caso de alerta, sigue las indicaciones de Protección Civil</li>
            </ul>
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- Seasonal Alert: Fire Season (Forest stations) -->
  {esForestal && temporadaSeca && (
    <section class="est-seasonal est-seasonal--fire">
      <div class="container">
        <div class="est-seasonal__card">
          <div class="est-seasonal__icon">🔥</div>
          <div class="est-seasonal__content">
            <h2>Alto Riesgo de Incendios Forestales</h2>
            <p>
              La temporada seca (febrero-mayo) presenta <strong>alto riesgo de incendios forestales</strong>
              en la Sierra Norte y zonas boscosas de Oaxaca.
            </p>
            <ul>
              <li>No realices quemas agrícolas sin autorización</li>
              <li>No arrojes colillas de cigarro en áreas naturales</li>
              <li>Reporta columnas de humo al 911 inmediatamente</li>
              <li>Colabora con las brigadas de combate de incendios</li>
            </ul>
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- Services & Equipment -->
  <section class="est-services">
    <div class="container">
      <div class="est-services__grid">
        <!-- Services -->
        <div class="est-services__block">
          <h2>Servicios de Emergencia</h2>
          <ul class="est-services__list">
            {estacion.servicios.map(servicio => (
              <li>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                  <path d="M22 4L12 14.01l-3-3"/>
                </svg>
                {servicio}
              </li>
            ))}
          </ul>
        </div>

        <!-- Equipment -->
        {estacion.equipamiento && estacion.equipamiento.length > 0 && (
          <div class="est-services__block">
            <h2>Equipamiento</h2>
            <ul class="est-services__list est-services__list--equipment">
              {estacion.equipamiento.map(equipo => (
                <li>
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="1" y="3" width="15" height="13"/>
                    <polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/>
                    <circle cx="5.5" cy="18.5" r="2.5"/>
                    <circle cx="18.5" cy="18.5" r="2.5"/>
                  </svg>
                  {equipo}
                </li>
              ))}
            </ul>
          </div>
        )}
      </div>
    </div>
  </section>

  <!-- Coverage Area -->
  {estacion.municipiosCubiertos && estacion.municipiosCubiertos.length > 0 && (
    <section class="est-coverage">
      <div class="container">
        <h2>Área de Cobertura</h2>
        <div class="est-coverage__grid">
          {estacion.municipiosCubiertos.map(municipio => (
            <div class="est-coverage__item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                <circle cx="12" cy="10" r="3"/>
              </svg>
              {municipio}
            </div>
          ))}
        </div>
        {estacion.caracteristicasEspeciales && estacion.caracteristicasEspeciales.length > 0 && (
          <div class="est-coverage__features">
            <h3>Características Especiales</h3>
            <div class="est-coverage__features-grid">
              {estacion.caracteristicasEspeciales.map(feature => (
                <span class="est-coverage__feature">{feature}</span>
              ))}
            </div>
          </div>
        )}
      </div>
    </section>
  )}

  <!-- Map -->
  <section class="est-map">
    <div class="container">
      <h2>Ubicación</h2>
      <div class="est-map__container">
        <iframe
          src={`https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3000!2d${estacion.coordenadas.lng}!3d${estacion.coordenadas.lat}!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zM${estacion.coordenadas.lat}!5e0!3m2!1ses!2smx!4v1`}
          width="100%"
          height="400"
          style="border:0;"
          allowfullscreen=""
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title={`Mapa de ${estacion.nombre}`}
        ></iframe>
      </div>
      <div class="est-map__actions">
        <a
          href={`https://www.google.com/maps/dir/?api=1&destination=${estacion.coordenadas.lat},${estacion.coordenadas.lng}`}
          target="_blank"
          rel="noopener noreferrer"
          class="est-map__btn"
        >
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polygon points="3 11 22 2 13 21 11 13 3 11"/>
          </svg>
          Cómo llegar
        </a>
        <a
          href={`https://www.google.com/maps/search/?api=1&query=${estacion.coordenadas.lat},${estacion.coordenadas.lng}`}
          target="_blank"
          rel="noopener noreferrer"
          class="est-map__btn est-map__btn--secondary"
        >
          Ver en Google Maps
        </a>
      </div>
    </div>
  </section>

  <!-- Other Stations in Zone -->
  {estacionesMismaZona.length > 0 && (
    <section class="est-related">
      <div class="container">
        <h2>Otras Estaciones en {estacion.zona}</h2>
        <div class="est-related__grid">
          {estacionesMismaZona.map(e => (
            <a href={`/directorio/oaxaca/${e.slug}`} class="est-related__card">
              <h3>{e.nombre.replace("Estación de Bomberos ", "").replace("Estación Central ", "")}</h3>
              <p>{e.direccion}</p>
              <span class="est-related__phone">{e.telefono}</span>
            </a>
          ))}
        </div>
        <a href="/directorio/oaxaca" class="est-related__all">
          Ver todas las estaciones en Oaxaca
        </a>
      </div>
    </section>
  )}

  <!-- Interlinking: Neighboring States -->
  <section class="est-interlinking">
    <div class="container">
      <h2>Bomberos en Estados Vecinos</h2>
      <p class="est-interlinking__desc">
        Coordinación regional con cuerpos de bomberos de estados limítrofes a Oaxaca
      </p>
      <div class="est-interlinking__grid">
        {estadosVecinos.map(estado => (
          <a href={`/directorio/${estado.slug}`} class="est-interlinking__card">
            <h3>{estado.estado}</h3>
            <p>{estado.relacion}</p>
            <span>Ver estaciones</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Interlinking: Products -->
  <section class="est-products">
    <div class="container">
      <h2>Equipamiento para Bomberos</h2>
      <p class="est-products__desc">
        Equipo certificado NFPA para las necesidades de los bomberos de Oaxaca
      </p>
      <div class="est-products__grid">
        {productosRelacionados.slice(0, 4).map(producto => (
          <a href={producto.slug} class="est-products__card">
            <h3>{producto.producto}</h3>
            <p>{producto.contexto}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Back to Directory -->
  <section class="est-back">
    <div class="container">
      <a href="/directorio/oaxaca" class="est-back__link">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M19 12H5M12 19l-7-7 7-7"/>
        </svg>
        Volver al directorio de Oaxaca
      </a>
    </div>
  </section>
</PageLayout>

<style>
  /* ═══════════════════════════════════════════════════════════════════════════
     OAXACA - PÁGINA DE ESTACIÓN INDIVIDUAL
     ═══════════════════════════════════════════════════════════════════════════ */

  :root {
    --oax-primary: #7c3aed;
    --oax-primary-dark: #6d28d9;
    --oax-primary-light: #a78bfa;
    --oax-accent: #f59e0b;
    --oax-unesco: #0ea5e9;
  }

  /* ═══ Hero Section ═══ */
  .est-hero {
    position: relative;
    padding: var(--space-12) 0 var(--space-16);
    background: linear-gradient(135deg, #1e1b4b 0%, #312e81 50%, #4c1d95 100%);
    overflow: hidden;
  }

  .est-hero__bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .est-hero__gradient {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 60% 40% at 20% 20%, var(--zone-color, rgba(124, 58, 237, 0.3)), transparent),
      radial-gradient(ellipse 50% 30% at 80% 80%, rgba(245, 158, 11, 0.2), transparent);
  }

  /* Breadcrumb */
  .est-breadcrumb ol {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-2);
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .est-breadcrumb li {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.6);
  }

  .est-breadcrumb li:not(:last-child)::after {
    content: "/";
    color: rgba(255, 255, 255, 0.3);
  }

  .est-breadcrumb a {
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
  }

  .est-breadcrumb a:hover {
    color: white;
  }

  .est-breadcrumb [aria-current="page"] {
    color: var(--oax-primary-light);
    font-weight: 600;
  }

  /* Alerts */
  .est-alerts {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    margin-bottom: var(--space-6);
  }

  .est-alert {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-lg);
    font-size: var(--text-sm);
  }

  .est-alert--warning {
    background: rgba(245, 158, 11, 0.15);
    border: 1px solid rgba(245, 158, 11, 0.3);
    color: #fbbf24;
  }

  .est-alert--danger {
    background: rgba(239, 68, 68, 0.15);
    border: 1px solid rgba(239, 68, 68, 0.3);
    color: #f87171;
  }

  .est-alert--info {
    background: rgba(59, 130, 246, 0.15);
    border: 1px solid rgba(59, 130, 246, 0.3);
    color: #60a5fa;
  }

  .est-alert__icon {
    font-size: var(--text-lg);
  }

  /* Hero Grid */
  .est-hero__grid {
    display: grid;
    grid-template-columns: 1fr 380px;
    gap: var(--space-10);
    align-items: start;
  }

  /* Badges */
  .est-hero__badges {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
  }

  .est-hero__badge {
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    font-weight: 600;
  }

  .est-hero__badge--zone {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
  }

  .est-hero__badge--unesco {
    background: rgba(14, 165, 233, 0.2);
    border: 1px solid rgba(14, 165, 233, 0.4);
    color: #7dd3fc;
  }

  .est-hero__badge--hazmat {
    background: rgba(239, 68, 68, 0.2);
    border: 1px solid rgba(239, 68, 68, 0.4);
    color: #fca5a5;
  }

  .est-hero__badge--aquatic {
    background: rgba(6, 182, 212, 0.2);
    border: 1px solid rgba(6, 182, 212, 0.4);
    color: #67e8f9;
  }

  .est-hero__badge--airport {
    background: rgba(168, 85, 247, 0.2);
    border: 1px solid rgba(168, 85, 247, 0.4);
    color: #d8b4fe;
  }

  .est-hero__badge--forest {
    background: rgba(34, 197, 94, 0.2);
    border: 1px solid rgba(34, 197, 94, 0.4);
    color: #86efac;
  }

  .est-hero__title {
    font-size: clamp(1.75rem, 3vw, 2.5rem);
    font-weight: 800;
    color: white;
    line-height: 1.2;
    margin: 0 0 var(--space-4);
  }

  .est-hero__address {
    display: flex;
    align-items: flex-start;
    gap: var(--space-2);
    font-style: normal;
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: var(--space-4);
    line-height: 1.5;
  }

  .est-hero__address svg {
    flex-shrink: 0;
    margin-top: 2px;
  }

  .est-hero__desc {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.7;
    margin: 0 0 var(--space-6);
  }

  /* Stats */
  .est-hero__stats {
    display: flex;
    gap: var(--space-6);
    padding-top: var(--space-6);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .est-hero__stat {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .est-hero__stat-value {
    font-size: var(--text-2xl);
    font-weight: 800;
    color: var(--oax-primary-light);
  }

  .est-hero__stat-label {
    font-size: var(--text-xs);
    color: rgba(255, 255, 255, 0.6);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Emergency Card */
  .est-emergency-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-2xl);
    padding: var(--space-6);
    text-align: center;
  }

  .est-emergency-card__header {
    font-size: var(--text-sm);
    font-weight: 600;
    color: rgba(255, 255, 255, 0.7);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-2);
  }

  .est-emergency-card__main {
    display: block;
    font-size: 4rem;
    font-weight: 900;
    color: var(--color-fire-400);
    text-decoration: none;
    line-height: 1;
    margin-bottom: var(--space-4);
  }

  .est-emergency-card__local {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-3);
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-lg);
    margin-bottom: var(--space-2);
  }

  .est-emergency-card__local span {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.6);
  }

  .est-emergency-card__local a {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--oax-primary-light);
    text-decoration: none;
  }

  .est-emergency-card__hours {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    margin-top: var(--space-4);
    padding-top: var(--space-4);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.7);
  }

  /* ═══ Bilingual Section ═══ */
  .est-bilingual {
    padding: var(--space-8) 0;
    background: linear-gradient(135deg, #0c4a6e 0%, #0369a1 100%);
  }

  .est-bilingual__card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
  }

  .est-bilingual__header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }

  .est-bilingual__flag {
    padding: var(--space-1) var(--space-2);
    background: white;
    border-radius: var(--radius-sm);
    font-size: var(--text-xs);
    font-weight: 700;
    color: #0369a1;
  }

  .est-bilingual__title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: white;
  }

  .est-bilingual__content p {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.7;
    margin: 0 0 var(--space-4);
  }

  .est-bilingual__content p:last-child {
    margin-bottom: 0;
  }

  .est-bilingual__content strong {
    color: white;
  }

  /* ═══ HAZMAT Section ═══ */
  .est-hazmat {
    padding: var(--space-10) 0;
    background: linear-gradient(135deg, #7f1d1d 0%, #991b1b 100%);
  }

  .est-hazmat__card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
  }

  .est-hazmat__header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }

  .est-hazmat__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: var(--radius-lg);
    color: #fbbf24;
  }

  .est-hazmat__header h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: white;
    margin: 0;
  }

  .est-hazmat__content p {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.7;
    margin: 0 0 var(--space-6);
  }

  .est-hazmat__facility {
    background: rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    padding: var(--space-4);
    margin-bottom: var(--space-4);
  }

  .est-hazmat__facility h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: white;
    margin: 0 0 var(--space-2);
  }

  .est-hazmat__facility ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .est-hazmat__facility li {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.8);
    padding: var(--space-1) 0;
  }

  /* ═══ UNESCO Section ═══ */
  .est-unesco {
    padding: var(--space-10) 0;
    background: linear-gradient(135deg, #0c4a6e 0%, #0369a1 100%);
  }

  .est-unesco__card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
  }

  .est-unesco__header {
    margin-bottom: var(--space-6);
  }

  .est-unesco__badge {
    display: inline-block;
    padding: var(--space-2) var(--space-4);
    background: rgba(14, 165, 233, 0.3);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: 600;
    color: #7dd3fc;
    margin-bottom: var(--space-3);
  }

  .est-unesco__header h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: white;
    margin: 0;
  }

  .est-unesco__content > p {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.7;
    margin: 0 0 var(--space-6);
  }

  .est-unesco__sites {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
  }

  .est-unesco__site {
    background: rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    padding: var(--space-4);
  }

  .est-unesco__site-year {
    font-size: var(--text-xs);
    color: #7dd3fc;
    font-weight: 600;
  }

  .est-unesco__site h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: white;
    margin: var(--space-2) 0;
  }

  .est-unesco__site p {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.7);
    margin: 0 0 var(--space-2);
  }

  .est-unesco__site-visitors {
    font-size: var(--text-xs);
    color: rgba(255, 255, 255, 0.6);
  }

  /* ═══ Seasonal Alerts ═══ */
  .est-seasonal {
    padding: var(--space-8) 0;
  }

  .est-seasonal--hurricane {
    background: linear-gradient(135deg, #1e3a5f 0%, #0d4f8b 100%);
  }

  .est-seasonal--fire {
    background: linear-gradient(135deg, #7f1d1d 0%, #b91c1c 100%);
  }

  .est-seasonal__card {
    display: flex;
    gap: var(--space-6);
    padding: var(--space-6);
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-xl);
  }

  .est-seasonal__icon {
    font-size: 3rem;
    flex-shrink: 0;
  }

  .est-seasonal__content h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: white;
    margin: 0 0 var(--space-3);
  }

  .est-seasonal__content p {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
    margin: 0 0 var(--space-4);
  }

  .est-seasonal__content ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-2);
  }

  .est-seasonal__content li {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.8);
    padding-left: var(--space-4);
    position: relative;
  }

  .est-seasonal__content li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: rgba(255, 255, 255, 0.5);
  }

  /* ═══ Services Section ═══ */
  .est-services {
    padding: var(--space-12) 0;
    background: var(--color-smoke-50);
  }

  .est-services__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
  }

  .est-services__block h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-6);
    padding-bottom: var(--space-3);
    border-bottom: 2px solid var(--oax-primary);
  }

  .est-services__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .est-services__list li {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    font-size: var(--text-base);
    color: var(--color-smoke-700);
    line-height: 1.5;
  }

  .est-services__list li svg {
    flex-shrink: 0;
    color: var(--oax-primary);
    margin-top: 2px;
  }

  .est-services__list--equipment li svg {
    color: var(--color-smoke-500);
  }

  /* ═══ Coverage Section ═══ */
  .est-coverage {
    padding: var(--space-12) 0;
    background: white;
  }

  .est-coverage h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-6);
    text-align: center;
  }

  .est-coverage__grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-3);
    margin-bottom: var(--space-8);
  }

  .est-coverage__item {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-4);
    background: var(--color-smoke-100);
    border-radius: var(--radius-lg);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-smoke-700);
  }

  .est-coverage__item svg {
    color: var(--oax-primary);
  }

  .est-coverage__features {
    text-align: center;
  }

  .est-coverage__features h3 {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-smoke-800);
    margin: 0 0 var(--space-4);
  }

  .est-coverage__features-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-2);
  }

  .est-coverage__feature {
    padding: var(--space-2) var(--space-4);
    background: rgba(124, 58, 237, 0.1);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--oax-primary);
  }

  /* ═══ Map Section ═══ */
  .est-map {
    padding: var(--space-12) 0;
    background: var(--color-smoke-50);
  }

  .est-map h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-6);
    text-align: center;
  }

  .est-map__container {
    border-radius: var(--radius-xl);
    overflow: hidden;
    margin-bottom: var(--space-6);
  }

  .est-map__actions {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
  }

  .est-map__btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    background: var(--oax-primary);
    color: white;
    font-size: var(--text-sm);
    font-weight: 600;
    text-decoration: none;
    border-radius: var(--radius-lg);
    transition: all 0.2s;
  }

  .est-map__btn:hover {
    background: var(--oax-primary-dark);
  }

  .est-map__btn--secondary {
    background: white;
    color: var(--oax-primary);
    border: 1px solid var(--color-smoke-200);
  }

  .est-map__btn--secondary:hover {
    background: var(--color-smoke-50);
  }

  /* ═══ Related Stations ═══ */
  .est-related {
    padding: var(--space-12) 0;
    background: white;
  }

  .est-related h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-6);
    text-align: center;
  }

  .est-related__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    margin-bottom: var(--space-6);
  }

  .est-related__card {
    padding: var(--space-5);
    background: var(--color-smoke-50);
    border-radius: var(--radius-xl);
    text-decoration: none;
    transition: all 0.2s;
    border: 1px solid transparent;
  }

  .est-related__card:hover {
    border-color: var(--oax-primary);
    box-shadow: 0 8px 24px rgba(124, 58, 237, 0.1);
  }

  .est-related__card h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-2);
  }

  .est-related__card p {
    font-size: var(--text-sm);
    color: var(--color-smoke-500);
    margin: 0 0 var(--space-3);
    line-height: 1.5;
  }

  .est-related__phone {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--oax-primary);
  }

  .est-related__all {
    display: block;
    text-align: center;
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--oax-primary);
    text-decoration: none;
  }

  .est-related__all:hover {
    text-decoration: underline;
  }

  /* ═══ Interlinking Section ═══ */
  .est-interlinking {
    padding: var(--space-12) 0;
    background: var(--color-smoke-50);
  }

  .est-interlinking h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-2);
    text-align: center;
  }

  .est-interlinking__desc {
    font-size: var(--text-base);
    color: var(--color-smoke-600);
    text-align: center;
    margin: 0 0 var(--space-8);
  }

  .est-interlinking__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-4);
  }

  .est-interlinking__card {
    padding: var(--space-5);
    background: white;
    border-radius: var(--radius-xl);
    text-decoration: none;
    border: 1px solid var(--color-smoke-200);
    transition: all 0.2s;
  }

  .est-interlinking__card:hover {
    border-color: var(--oax-primary);
    box-shadow: 0 4px 12px rgba(124, 58, 237, 0.1);
  }

  .est-interlinking__card h3 {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-smoke-900);
    margin: 0 0 var(--space-2);
  }

  .est-interlinking__card p {
    font-size: var(--text-sm);
    color: var(--color-smoke-600);
    margin: 0 0 var(--space-3);
    line-height: 1.5;
  }

  .est-interlinking__card span {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--oax-primary);
  }

  /* ═══ Products Section ═══ */
  .est-products {
    padding: var(--space-12) 0;
    background: linear-gradient(135deg, #1e1b4b 0%, #312e81 100%);
  }

  .est-products h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    color: white;
    margin: 0 0 var(--space-2);
    text-align: center;
  }

  .est-products__desc {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.7);
    text-align: center;
    margin: 0 0 var(--space-8);
  }

  .est-products__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-4);
  }

  .est-products__card {
    padding: var(--space-5);
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-xl);
    text-decoration: none;
    transition: all 0.2s;
  }

  .est-products__card:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px);
  }

  .est-products__card h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: white;
    margin: 0 0 var(--space-2);
  }

  .est-products__card p {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.7);
    margin: 0;
  }

  /* ═══ Back Section ═══ */
  .est-back {
    padding: var(--space-8) 0;
    background: white;
  }

  .est-back__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--oax-primary);
    text-decoration: none;
    transition: gap 0.2s;
  }

  .est-back__link:hover {
    gap: var(--space-3);
  }

  /* ═══ Responsive ═══ */
  @media (max-width: 1024px) {
    .est-hero__grid {
      grid-template-columns: 1fr;
    }

    .est-hero__emergency {
      max-width: 400px;
    }

    .est-services__grid {
      grid-template-columns: 1fr;
    }

    .est-interlinking__grid,
    .est-products__grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .est-unesco__sites {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .est-hero {
      padding: var(--space-8) 0 var(--space-12);
    }

    .est-related__grid,
    .est-interlinking__grid,
    .est-products__grid {
      grid-template-columns: 1fr;
    }

    .est-seasonal__card {
      flex-direction: column;
      text-align: center;
    }

    .est-seasonal__content ul {
      grid-template-columns: 1fr;
    }

    .est-hero__stats {
      flex-wrap: wrap;
      gap: var(--space-4);
    }

    .est-map__actions {
      flex-direction: column;
    }

    .est-map__btn {
      justify-content: center;
    }
  }
</style>
