---
/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * PÁGINA DINÁMICA - ESTACIÓN DE BOMBEROS EN QUERÉTARO
 * ═══════════════════════════════════════════════════════════════════════════════
 * El hub aeroespacial #1 de México
 * Centro Histórico UNESCO (1996) | Misiones Sierra Gorda (2003)
 * 4 Pueblos Mágicos | Ruta del Vino y Queso
 * ═══════════════════════════════════════════════════════════════════════════════
 */

import PageLayout from "@layouts/PageLayout.astro";
import {
  estacionesQueretaro,
  QUERETARO_SEO,
  ZONAS_QUERETARO,
  INDUSTRIA_QUERETARO,
  TURISMO_QUERETARO,
  INFRAESTRUCTURA_QUERETARO,
  FAQ_QUERETARO,
  ALERTAS_QUERETARO,
  getEstacionesPorZona,
  getEstacionesHAZMAT,
  getEstacionesAeroespaciales,
  getEstacionesPueblosMagicos,
  getEstacionesUNESCO,
  getAlertasActivas,
  generarSchemaEstacion,
  generarInterlinking
} from "@data/estaciones-queretaro";

// ─── Generación de Rutas Estáticas ───────────────────────────────────────────
export function getStaticPaths() {
  return estacionesQueretaro.map((estacion) => ({
    params: { slug: estacion.slug },
    props: { estacion }
  }));
}

// ─── Props y Datos ───────────────────────────────────────────────────────────
const { estacion } = Astro.props;

// Metadata dinámica
const title = `${estacion.nombre} | Bomberos Querétaro | BOMBERO.MX`;
const description = `${estacion.descripcion} Teléfono: ${estacion.telefono}. Horario: ${estacion.horario}. Cobertura: ${estacion.municipiosCubiertos?.join(", ")}.`;

// Clasificadores
const esHAZMAT = estacion.servicios?.some(s => s.toLowerCase().includes("hazmat")) ||
                  estacion.caracteristicasEspeciales?.some(c => c.toLowerCase().includes("hazmat"));
const esAeroespacial = estacion.caracteristicasEspeciales?.some(c =>
  c.toLowerCase().includes("aeroespacial") ||
  c.toLowerCase().includes("bombardier") ||
  c.toLowerCase().includes("safran")
);
const esUNESCO = estacion.caracteristicasEspeciales?.some(c => c.toLowerCase().includes("unesco"));
const esPuebloMagico = estacion.caracteristicasEspeciales?.some(c => c.toLowerCase().includes("pueblo mágico"));
const esRescateVertical = estacion.servicios?.some(s => s.toLowerCase().includes("vertical"));
const esCarretero = estacion.caracteristicasEspeciales?.some(c =>
  c.toLowerCase().includes("autopista") ||
  c.toLowerCase().includes("57d") ||
  c.toLowerCase().includes("carretero")
);
const esAeropuerto = estacion.servicios?.some(s =>
  s.toLowerCase().includes("aeronáutica") ||
  s.toLowerCase().includes("ssei") ||
  s.toLowerCase().includes("arff")
);
const esForestal = estacion.servicios?.some(s => s.toLowerCase().includes("forestal"));

// Zona de la estación
const zonaKey = Object.keys(ZONAS_QUERETARO).find(
  k => ZONAS_QUERETARO[k as keyof typeof ZONAS_QUERETARO].nombre === estacion.zona
) || 'centroHistorico';
const zonaInfo = ZONAS_QUERETARO[zonaKey as keyof typeof ZONAS_QUERETARO];

// Estaciones relacionadas
const estacionesZona = getEstacionesPorZona(estacion.zona).filter(e => e.slug !== estacion.slug);
const todasEstaciones = estacionesQueretaro.filter(e => e.slug !== estacion.slug);

// Alertas activas para la zona
const alertasZona = getAlertasActivas(estacion.zona);

// Interlinking
const { estados: estadosVecinos, productos: productosRelacionados } = generarInterlinking();

// Empresas aeroespaciales relacionadas (si aplica)
const empresasRelacionadas = esAeroespacial ? INDUSTRIA_QUERETARO.aeroespacial.empresasPrincipales : [];

// FAQ específicas para tipo de estación
const faqRelevantes = FAQ_QUERETARO.filter(faq => {
  const preguntaLower = faq.pregunta.toLowerCase();
  if (esHAZMAT && preguntaLower.includes("hazmat")) return true;
  if (esAeroespacial && preguntaLower.includes("aeroespacial")) return true;
  if (esUNESCO && preguntaLower.includes("unesco")) return true;
  if (esRescateVertical && preguntaLower.includes("bernal")) return true;
  if (esCarretero && preguntaLower.includes("autopista")) return true;
  if (esForestal && preguntaLower.includes("sierra")) return true;
  if (preguntaLower.includes("teléfono")) return true;
  return false;
}).slice(0, 4);

// Schema.org Ultra-Profesional Multi-tipo
const schemaEstacion = generarSchemaEstacion(estacion);
const schemaOrg = {
  "@context": "https://schema.org",
  "@graph": [
    schemaEstacion,
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Inicio", "item": "https://bombero.mx" },
        { "@type": "ListItem", "position": 2, "name": "Directorio", "item": "https://bombero.mx/directorio" },
        { "@type": "ListItem", "position": 3, "name": "Querétaro", "item": "https://bombero.mx/directorio/queretaro" },
        { "@type": "ListItem", "position": 4, "name": estacion.nombre, "item": `https://bombero.mx/directorio/queretaro/${estacion.slug}` }
      ]
    },
    {
      "@type": "GovernmentService",
      "name": `Servicios de ${estacion.nombre}`,
      "serviceType": estacion.servicios,
      "provider": {
        "@type": "GovernmentOrganization",
        "name": "H. Cuerpo de Bomberos de Querétaro"
      },
      "areaServed": {
        "@type": "AdministrativeArea",
        "name": estacion.municipiosCubiertos?.join(", ")
      },
      "availableChannel": {
        "@type": "ServiceChannel",
        "servicePhone": estacion.telefono,
        "availableLanguage": ["Spanish", esAeroespacial || estacion.zona?.includes("Industrial") ? "English" : null].filter(Boolean)
      }
    },
    ...(faqRelevantes.length > 0 ? [{
      "@type": "FAQPage",
      "mainEntity": faqRelevantes.map(faq => ({
        "@type": "Question",
        "name": faq.pregunta,
        "acceptedAnswer": { "@type": "Answer", "text": faq.respuesta }
      }))
    }] : []),
    ...(esAeroespacial ? [{
      "@type": "LocalBusiness",
      "name": estacion.nombre,
      "@id": `https://bombero.mx/directorio/queretaro/${estacion.slug}#industrial`,
      "description": "Estación de bomberos especializada en emergencias del clúster aeroespacial",
      "knowsAbout": ["HAZMAT", "Aerospace Industry", "Composite Materials", "Aviation Fuels"]
    }] : []),
    ...(esUNESCO || esPuebloMagico ? [{
      "@type": "TouristDestination",
      "name": estacion.zona,
      "description": zonaInfo?.descripcion,
      "touristType": ["Cultural Tourism", "Heritage Tourism"]
    }] : [])
  ]
};

// Keywords dinámicas según tipo
const keywordsBase = [
  `bomberos ${estacion.nombre.replace("Estación de Bomberos ", "").replace("Estación Central ", "")}`,
  `bomberos Querétaro ${estacion.zona}`,
  `emergencias ${estacion.municipiosCubiertos?.[0]}`,
  "911 Querétaro"
];

const keywordsEspeciales = [
  ...(esHAZMAT ? ["HAZMAT Querétaro", "materiales peligrosos industria"] : []),
  ...(esAeroespacial ? ["bomberos aeroespacial", "emergencias Bombardier", "HAZMAT aeronáutico"] : []),
  ...(esUNESCO ? ["protección patrimonio UNESCO", "bomberos Centro Histórico"] : []),
  ...(esPuebloMagico ? ["bomberos Pueblo Mágico", "emergencias turísticas"] : []),
  ...(esRescateVertical ? ["rescate vertical Peña Bernal", "rescate escaladores"] : []),
  ...(esCarretero ? ["rescate carretero 57D", "accidentes autopista México-Querétaro"] : []),
  ...(esAeropuerto ? ["SSEI aeropuerto", "bomberos aeronáuticos", "ARFF Querétaro"] : []),
  ...(esForestal ? ["incendios forestales Sierra Gorda", "bomberos reserva biósfera"] : [])
];

const allKeywords = [...keywordsBase, ...keywordsEspeciales].join(", ");
---

<PageLayout title={title} description={description}>
  <script type="application/ld+json" set:html={JSON.stringify(schemaOrg)} />
  <meta name="keywords" content={allKeywords} slot="head" />
  <meta name="geo.region" content="MX-QUE" slot="head" />
  <meta name="geo.placename" content={estacion.municipiosCubiertos?.[0] || "Querétaro"} slot="head" />
  <meta name="geo.position" content={`${estacion.coordenadas.lat};${estacion.coordenadas.lng}`} slot="head" />

  <!-- Hero Section Premium -->
  <section class="qro-detail-hero" style={`--zone-color: ${zonaInfo?.color || '#0ea5e9'}`}>
    <div class="qro-detail-hero__bg">
      <div class="qro-detail-hero__gradient"></div>
      <div class="qro-detail-hero__pattern"></div>
    </div>

    <div class="container">
      <!-- Breadcrumb -->
      <nav class="qro-detail-breadcrumb" aria-label="Breadcrumb">
        <ol>
          <li><a href="/">Inicio</a></li>
          <li><a href="/directorio">Directorio</a></li>
          <li><a href="/directorio/queretaro">Querétaro</a></li>
          <li aria-current="page">{estacion.nombre.replace("Estación de Bomberos ", "").replace("Estación Central ", "")}</li>
        </ol>
      </nav>

      <!-- Alertas Contextuales -->
      {alertasZona.length > 0 && (
        <div class="qro-detail-alerts">
          {alertasZona.slice(0, 2).map(alerta => (
            <div class={`qro-detail-alert qro-detail-alert--${alerta.tipo}`}>
              <span class="qro-detail-alert__icon">{alerta.icono}</span>
              <span class="qro-detail-alert__text">{alerta.mensaje}</span>
            </div>
          ))}
        </div>
      )}

      <div class="qro-detail-hero__grid">
        <div class="qro-detail-hero__content">
          <!-- Badges -->
          <div class="qro-detail-badges">
            <span class="qro-detail-badge qro-detail-badge--zone">{estacion.zona}</span>
            {esHAZMAT && <span class="qro-detail-badge qro-detail-badge--hazmat">HAZMAT</span>}
            {esAeroespacial && <span class="qro-detail-badge qro-detail-badge--aero">Aeroespacial</span>}
            {esUNESCO && <span class="qro-detail-badge qro-detail-badge--unesco">UNESCO</span>}
            {esPuebloMagico && <span class="qro-detail-badge qro-detail-badge--magic">Pueblo Mágico</span>}
            {esRescateVertical && <span class="qro-detail-badge qro-detail-badge--vertical">Rescate Vertical</span>}
            {esAeropuerto && <span class="qro-detail-badge qro-detail-badge--airport">SSEI</span>}
            {esCarretero && <span class="qro-detail-badge qro-detail-badge--highway">Carretero</span>}
            {esForestal && <span class="qro-detail-badge qro-detail-badge--forestal">Forestal</span>}
          </div>

          <h1 class="qro-detail-hero__title">{estacion.nombre}</h1>

          <p class="qro-detail-hero__desc">{estacion.descripcion}</p>

          <div class="qro-detail-hero__address">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>
            </svg>
            <span>{estacion.direccion}</span>
          </div>

          <div class="qro-detail-hero__meta">
            <div class="qro-detail-hero__meta-item">
              <span class="qro-detail-hero__meta-label">Horario</span>
              <span class="qro-detail-hero__meta-value">{estacion.horario}</span>
            </div>
            {estacion.personal && (
              <div class="qro-detail-hero__meta-item">
                <span class="qro-detail-hero__meta-label">Personal</span>
                <span class="qro-detail-hero__meta-value">{estacion.personal} bomberos</span>
              </div>
            )}
            {estacion.tiempoRespuesta && (
              <div class="qro-detail-hero__meta-item">
                <span class="qro-detail-hero__meta-label">Tiempo Respuesta</span>
                <span class="qro-detail-hero__meta-value">{estacion.tiempoRespuesta}</span>
              </div>
            )}
            <div class="qro-detail-hero__meta-item">
              <span class="qro-detail-hero__meta-label">Copias de Acta</span>
              <span class="qro-detail-hero__meta-value">{estacion.copiasActa ? "Sí disponible" : "No disponible"}</span>
            </div>
          </div>

          <!-- Bilingual Notice para estaciones industriales -->
          {(esAeroespacial || estacion.zona?.includes("Industrial")) && (
            <div class="qro-detail-bilingual">
              <div class="qro-detail-bilingual__header">
                <span class="qro-detail-bilingual__flag">EN</span>
                <span>International Business Coverage</span>
              </div>
              <p>
                {esAeroespacial
                  ? "Specialized HAZMAT response for aerospace industry (Bombardier, Safran, ITP Aero). Level A certified personnel for composite materials, aviation fuels, and epoxy resins."
                  : "Industrial emergency response with English-speaking coordination available for international companies."}
              </p>
            </div>
          )}
        </div>

        <!-- Emergency Card -->
        <div class="qro-detail-emergency">
          <div class="qro-detail-emergency__main">
            <span class="qro-detail-emergency__label">Línea de Emergencia</span>
            <a href="tel:911" class="qro-detail-emergency__911">911</a>
            <span class="qro-detail-emergency__note">Emergencias / Emergency</span>
          </div>

          <div class="qro-detail-emergency__local">
            <span class="qro-detail-emergency__local-label">Teléfono Directo</span>
            <a href={`tel:${estacion.telefono.replace(/[^\d+]/g, '')}`} class="qro-detail-emergency__phone">
              {estacion.telefono}
            </a>
          </div>

          {estacion.telefonoEmergencias && estacion.telefonoEmergencias !== "911" && (
            <div class="qro-detail-emergency__alt">
              <span>Emergencias Local:</span>
              <a href={`tel:${estacion.telefonoEmergencias.replace(/[^\d+]/g, '')}`}>{estacion.telefonoEmergencias}</a>
            </div>
          )}

          <div class="qro-detail-emergency__actions">
            <a
              href={`https://www.google.com/maps/dir/?api=1&destination=${estacion.coordenadas.lat},${estacion.coordenadas.lng}`}
              target="_blank"
              rel="noopener noreferrer"
              class="qro-detail-emergency__action qro-detail-emergency__action--directions"
            >
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polygon points="3 11 22 2 13 21 11 13 3 11"/>
              </svg>
              <span>Cómo llegar</span>
            </a>
            {estacion.sitioWeb && (
              <a
                href={estacion.sitioWeb}
                target="_blank"
                rel="noopener noreferrer"
                class="qro-detail-emergency__action"
              >
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                </svg>
                <span>Sitio Web</span>
              </a>
            )}
            {estacion.facebook && (
              <a
                href={estacion.facebook}
                target="_blank"
                rel="noopener noreferrer"
                class="qro-detail-emergency__action"
              >
                <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/>
                </svg>
                <span>Facebook</span>
              </a>
            )}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="qro-detail-services">
    <div class="container">
      <div class="qro-detail-section-header">
        <span class="qro-detail-section-label">Capacidades</span>
        <h2>Servicios Especializados</h2>
      </div>

      <div class="qro-detail-services__grid">
        {estacion.servicios?.map((servicio, index) => {
          const esServicioHAZMAT = servicio.toLowerCase().includes("hazmat");
          const esServicioRescate = servicio.toLowerCase().includes("rescate");
          const esServicioIncendio = servicio.toLowerCase().includes("incendio") || servicio.toLowerCase().includes("combate");
          const esServicioMedico = servicio.toLowerCase().includes("hospitalaria") || servicio.toLowerCase().includes("médica");

          return (
            <div class={`qro-detail-service ${esServicioHAZMAT ? 'qro-detail-service--hazmat' : ''} ${esServicioRescate ? 'qro-detail-service--rescue' : ''} ${esServicioMedico ? 'qro-detail-service--medical' : ''}`}>
              <div class="qro-detail-service__icon">
                {esServicioHAZMAT && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
                    <line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>
                  </svg>
                )}
                {esServicioRescate && !esServicioHAZMAT && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"/><path d="M16 8l-4 4-4-4"/><path d="M12 12v6"/>
                  </svg>
                )}
                {esServicioIncendio && !esServicioHAZMAT && !esServicioRescate && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/>
                  </svg>
                )}
                {esServicioMedico && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M22 12h-4l-3 9L9 3l-3 9H2"/>
                  </svg>
                )}
                {!esServicioHAZMAT && !esServicioRescate && !esServicioIncendio && !esServicioMedico && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/>
                  </svg>
                )}
              </div>
              <span class="qro-detail-service__name">{servicio}</span>
              <span class="qro-detail-service__number">{String(index + 1).padStart(2, '0')}</span>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- Equipment Section -->
  {estacion.equipamiento && estacion.equipamiento.length > 0 && (
    <section class="qro-detail-equipment">
      <div class="container">
        <div class="qro-detail-section-header">
          <span class="qro-detail-section-label">Flota y Equipamiento</span>
          <h2>Recursos Disponibles</h2>
        </div>

        <div class="qro-detail-equipment__grid">
          {estacion.equipamiento.map(equipo => {
            const esVehiculo = equipo.toLowerCase().includes("autobomba") ||
                               equipo.toLowerCase().includes("ambulancia") ||
                               equipo.toLowerCase().includes("unidad") ||
                               equipo.toLowerCase().includes("vehículo") ||
                               equipo.toLowerCase().includes("escalera");
            const esHazmat = equipo.toLowerCase().includes("hazmat") ||
                             equipo.toLowerCase().includes("encapsulado") ||
                             equipo.toLowerCase().includes("detector");
            const esRescate = equipo.toLowerCase().includes("rescate") ||
                              equipo.toLowerCase().includes("cuerdas") ||
                              equipo.toLowerCase().includes("hidráulica");

            return (
              <div class={`qro-detail-equipment__item ${esVehiculo ? 'qro-detail-equipment__item--vehicle' : ''} ${esHazmat ? 'qro-detail-equipment__item--hazmat' : ''} ${esRescate ? 'qro-detail-equipment__item--rescue' : ''}`}>
                <div class="qro-detail-equipment__icon">
                  {esVehiculo && (
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/>
                      <circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/>
                    </svg>
                  )}
                  {esHazmat && !esVehiculo && (
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                      <polyline points="14 2 14 8 20 8"/><line x1="12" y1="18" x2="12" y2="12"/><line x1="9" y1="15" x2="15" y2="15"/>
                    </svg>
                  )}
                  {esRescate && !esVehiculo && !esHazmat && (
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/>
                    </svg>
                  )}
                  {!esVehiculo && !esHazmat && !esRescate && (
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
                    </svg>
                  )}
                </div>
                <span class="qro-detail-equipment__name">{equipo}</span>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  )}

  <!-- Aerospace Companies Section (si aplica) -->
  {esAeroespacial && empresasRelacionadas.length > 0 && (
    <section class="qro-detail-aerospace">
      <div class="container">
        <div class="qro-detail-section-header">
          <span class="qro-detail-section-label">Cobertura Industrial</span>
          <h2>Empresas Aeroespaciales en Zona de Cobertura</h2>
        </div>

        <div class="qro-detail-aerospace__grid">
          {empresasRelacionadas.map(empresa => (
            <div class="qro-detail-aerospace__card">
              <h3>{empresa.nombre}</h3>
              <p class="qro-detail-aerospace__location">{empresa.ubicacion}</p>
              <div class="qro-detail-aerospace__details">
                <span><strong>{empresa.empleados.toLocaleString()}</strong> empleados</span>
                <span>{empresa.actividad}</span>
              </div>
              <div class="qro-detail-aerospace__risk">
                <span>Riesgo HAZMAT:</span> {empresa.riesgo}
              </div>
            </div>
          ))}
        </div>

        <div class="qro-detail-aerospace__certifications">
          <h4>Certificaciones del Clúster Aeroespacial</h4>
          <div class="qro-detail-aerospace__cert-grid">
            {INDUSTRIA_QUERETARO.aeroespacial.certificaciones.map(cert => (
              <span class="qro-detail-aerospace__cert">{cert}</span>
            ))}
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- Coverage Area -->
  <section class="qro-detail-coverage">
    <div class="container">
      <div class="qro-detail-coverage__grid">
        <div class="qro-detail-coverage__info">
          <div class="qro-detail-section-header qro-detail-section-header--left">
            <span class="qro-detail-section-label">Área de Servicio</span>
            <h2>Cobertura Geográfica</h2>
          </div>

          <div class="qro-detail-coverage__zone">
            <h3 style={`border-color: ${zonaInfo?.color}`}>{zonaInfo?.nombre}</h3>
            <p>{zonaInfo?.descripcion}</p>

            <div class="qro-detail-coverage__characteristics">
              {zonaInfo?.caracteristicas?.map(c => (
                <span class="qro-detail-coverage__char">{c}</span>
              ))}
            </div>
          </div>

          <div class="qro-detail-coverage__municipalities">
            <h4>Municipios y Zonas Cubiertas</h4>
            <ul>
              {estacion.municipiosCubiertos?.map(municipio => (
                <li>{municipio}</li>
              ))}
            </ul>
          </div>

          {zonaInfo?.riesgosPrincipales && (
            <div class="qro-detail-coverage__risks">
              <h4>Riesgos Principales de la Zona</h4>
              <div class="qro-detail-coverage__risks-list">
                {zonaInfo.riesgosPrincipales.map(r => (
                  <span class="qro-detail-coverage__risk-tag">{r}</span>
                ))}
              </div>
            </div>
          )}
        </div>

        <div class="qro-detail-coverage__map">
          <div class="qro-detail-map">
            <iframe
              src={`https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3000!2d${estacion.coordenadas.lng}!3d${estacion.coordenadas.lat}!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zM!5e0!3m2!1ses!2smx!4v1`}
              width="100%"
              height="400"
              style="border:0; border-radius: 16px;"
              allowfullscreen=""
              loading="lazy"
              referrerpolicy="no-referrer-when-downgrade"
              title={`Mapa de ${estacion.nombre}`}
            ></iframe>
            <div class="qro-detail-map__coords">
              <span>Coordenadas:</span>
              <code>{estacion.coordenadas.lat.toFixed(4)}, {estacion.coordenadas.lng.toFixed(4)}</code>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Características Especiales -->
  {estacion.caracteristicasEspeciales && estacion.caracteristicasEspeciales.length > 0 && (
    <section class="qro-detail-features">
      <div class="container">
        <div class="qro-detail-section-header">
          <span class="qro-detail-section-label">Distintivos</span>
          <h2>Características Especiales</h2>
        </div>

        <div class="qro-detail-features__grid">
          {estacion.caracteristicasEspeciales.map(caracteristica => (
            <div class="qro-detail-feature">
              <div class="qro-detail-feature__icon">
                {caracteristica.toLowerCase().includes("hazmat") && "☣️"}
                {caracteristica.toLowerCase().includes("unesco") && "🏛️"}
                {caracteristica.toLowerCase().includes("pueblo mágico") && "✨"}
                {caracteristica.toLowerCase().includes("aeroespacial") && "✈️"}
                {caracteristica.toLowerCase().includes("bombardier") && "🛫"}
                {caracteristica.toLowerCase().includes("peña") && "⛰️"}
                {caracteristica.toLowerCase().includes("autopista") && "🛣️"}
                {caracteristica.toLowerCase().includes("aeropuerto") && "🛬"}
                {caracteristica.toLowerCase().includes("forestal") && "🌲"}
                {caracteristica.toLowerCase().includes("vino") && "🍷"}
                {!caracteristica.toLowerCase().includes("hazmat") &&
                 !caracteristica.toLowerCase().includes("unesco") &&
                 !caracteristica.toLowerCase().includes("pueblo mágico") &&
                 !caracteristica.toLowerCase().includes("aeroespacial") &&
                 !caracteristica.toLowerCase().includes("bombardier") &&
                 !caracteristica.toLowerCase().includes("peña") &&
                 !caracteristica.toLowerCase().includes("autopista") &&
                 !caracteristica.toLowerCase().includes("aeropuerto") &&
                 !caracteristica.toLowerCase().includes("forestal") &&
                 !caracteristica.toLowerCase().includes("vino") && "⭐"}
              </div>
              <span class="qro-detail-feature__text">{caracteristica}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- FAQ Section -->
  {faqRelevantes.length > 0 && (
    <section class="qro-detail-faq">
      <div class="container">
        <div class="qro-detail-section-header">
          <span class="qro-detail-section-label">Información</span>
          <h2>Preguntas Frecuentes</h2>
        </div>

        <div class="qro-detail-faq__grid">
          {faqRelevantes.map((faq, index) => (
            <details class="qro-detail-faq__item" open={index === 0}>
              <summary>{faq.pregunta}</summary>
              <p>{faq.respuesta}</p>
            </details>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Related Stations -->
  <section class="qro-detail-related">
    <div class="container">
      <div class="qro-detail-section-header">
        <span class="qro-detail-section-label">{estacion.zona}</span>
        <h2>Otras Estaciones en la Zona</h2>
      </div>

      {estacionesZona.length > 0 ? (
        <div class="qro-detail-related__grid">
          {estacionesZona.slice(0, 3).map(e => (
            <a href={`/directorio/queretaro/${e.slug}`} class="qro-detail-related__card">
              <h3>{e.nombre.replace("Estación de Bomberos ", "").replace("Estación Central ", "")}</h3>
              <p>{e.direccion}</p>
              <div class="qro-detail-related__meta">
                <span class="qro-detail-related__phone">{e.telefono}</span>
                {e.personal && <span>{e.personal} bomberos</span>}
              </div>
            </a>
          ))}
        </div>
      ) : (
        <p class="qro-detail-related__empty">Esta es la única estación en {estacion.zona}</p>
      )}

      <div class="qro-detail-related__all">
        <a href="/directorio/queretaro" class="qro-detail-btn qro-detail-btn--outline">
          Ver todas las estaciones de Querétaro
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Interlinking Section -->
  <section class="qro-detail-interlinking">
    <div class="container">
      <div class="qro-detail-interlinking__grid">
        <div class="qro-detail-interlinking__states">
          <h3>Bomberos en Estados Vecinos</h3>
          <div class="qro-detail-interlinking__states-grid">
            {estadosVecinos.slice(0, 4).map(estado => (
              <a href={`/directorio/${estado.slug}`} class="qro-detail-neighbor-link">
                <span class="qro-detail-neighbor-link__name">{estado.estado}</span>
                <span class="qro-detail-neighbor-link__context">{estado.contexto}</span>
              </a>
            ))}
          </div>
        </div>

        <div class="qro-detail-interlinking__products">
          <h3>Equipamiento Relacionado</h3>
          <div class="qro-detail-interlinking__products-grid">
            {productosRelacionados.filter(p => p.prioridad === 'critica' || p.prioridad === 'alta').slice(0, 4).map(producto => (
              <a href={producto.slug} class="qro-detail-product-link">
                <span class="qro-detail-product-link__name">{producto.producto}</span>
                <span class="qro-detail-product-link__context">{producto.contexto}</span>
              </a>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="qro-detail-cta">
    <div class="container">
      <div class="qro-detail-cta__content">
        <h2>¿Necesitas Equipamiento Especializado?</h2>
        <p>
          {esHAZMAT || esAeroespacial
            ? "Equipos HAZMAT Nivel A, detectores multigas y trajes encapsulados para industria aeroespacial"
            : esRescateVertical
            ? "Equipos de rescate vertical, cuerdas certificadas y arneses para rescate en altura"
            : esCarretero
            ? "Herramientas de rescate vehicular, estabilizadores y equipos de extricación"
            : "Equipamiento profesional para bomberos: trajes estructurales, SCBA y herramientas de rescate"}
        </p>
        <a href="/cotizar" class="qro-detail-cta__btn">
          Solicitar Cotización
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
          </svg>
        </a>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  /* ═══════════════════════════════════════════════════════════════════════════
     QUERÉTARO - PÁGINA DE DETALLE ULTRA-PROFESIONAL
     Theme: Azul aeroespacial con variantes dinámicas por zona
     ═══════════════════════════════════════════════════════════════════════════ */

  :root {
    --qro-primary: #0ea5e9;
    --qro-primary-dark: #0284c7;
    --qro-primary-light: #38bdf8;
    --qro-accent: #f59e0b;
    --qro-hazmat: #dc2626;
    --qro-unesco: #7c3aed;
    --qro-magic: #f59e0b;
    --qro-forestal: #16a34a;
  }

  /* ═══ Hero Section ═══ */
  .qro-detail-hero {
    position: relative;
    padding: var(--space-14) 0 var(--space-16);
    background: linear-gradient(135deg, #0c4a6e 0%, #075985 40%, var(--zone-color, #0ea5e9) 100%);
    overflow: hidden;
  }

  .qro-detail-hero__bg { position: absolute; inset: 0; pointer-events: none; }

  .qro-detail-hero__gradient {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 60% at 20% 30%, rgba(14, 165, 233, 0.3), transparent),
      radial-gradient(ellipse 60% 40% at 80% 70%, rgba(var(--zone-color), 0.2), transparent);
  }

  .qro-detail-hero__pattern {
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M30 0L60 30L30 60L0 30z' fill='none' stroke='%23ffffff08' stroke-width='1'/%3E%3C/svg%3E");
  }

  .qro-detail-breadcrumb { margin-bottom: var(--space-6); }
  .qro-detail-breadcrumb ol { display: flex; flex-wrap: wrap; gap: var(--space-2); list-style: none; padding: 0; margin: 0; }
  .qro-detail-breadcrumb li { display: flex; align-items: center; gap: var(--space-2); font-size: var(--text-sm); color: rgba(255,255,255,0.6); }
  .qro-detail-breadcrumb li:not(:last-child)::after { content: "/"; color: rgba(255,255,255,0.3); }
  .qro-detail-breadcrumb a { color: rgba(255,255,255,0.6); text-decoration: none; transition: color 0.2s; }
  .qro-detail-breadcrumb a:hover { color: white; }
  .qro-detail-breadcrumb [aria-current] { color: var(--qro-primary-light); font-weight: 600; }

  .qro-detail-alerts { display: flex; flex-direction: column; gap: var(--space-2); margin-bottom: var(--space-6); }
  .qro-detail-alert { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-3) var(--space-4); border-radius: var(--radius-lg); font-size: var(--text-sm); }
  .qro-detail-alert--info { background: rgba(14,165,233,0.15); border: 1px solid rgba(14,165,233,0.3); color: #7dd3fc; }
  .qro-detail-alert--warning { background: rgba(245,158,11,0.15); border: 1px solid rgba(245,158,11,0.3); color: #fbbf24; }
  .qro-detail-alert--danger { background: rgba(239,68,68,0.15); border: 1px solid rgba(239,68,68,0.3); color: #f87171; }

  .qro-detail-hero__grid { position: relative; display: grid; grid-template-columns: 1fr 380px; gap: var(--space-10); align-items: start; }

  .qro-detail-badges { display: flex; flex-wrap: wrap; gap: var(--space-2); margin-bottom: var(--space-4); }
  .qro-detail-badge { display: inline-flex; align-items: center; padding: var(--space-1) var(--space-3); border-radius: var(--radius-full); font-size: var(--text-xs); font-weight: 600; }
  .qro-detail-badge--zone { background: rgba(255,255,255,0.15); color: white; border: 1px solid rgba(255,255,255,0.25); }
  .qro-detail-badge--hazmat { background: rgba(220,38,38,0.2); color: #fca5a5; border: 1px solid rgba(220,38,38,0.4); }
  .qro-detail-badge--aero { background: rgba(14,165,233,0.2); color: #7dd3fc; border: 1px solid rgba(14,165,233,0.4); }
  .qro-detail-badge--unesco { background: rgba(124,58,237,0.2); color: #c4b5fd; border: 1px solid rgba(124,58,237,0.4); }
  .qro-detail-badge--magic { background: rgba(245,158,11,0.2); color: #fcd34d; border: 1px solid rgba(245,158,11,0.4); }
  .qro-detail-badge--vertical { background: rgba(16,185,129,0.2); color: #6ee7b7; border: 1px solid rgba(16,185,129,0.4); }
  .qro-detail-badge--airport { background: rgba(99,102,241,0.2); color: #a5b4fc; border: 1px solid rgba(99,102,241,0.4); }
  .qro-detail-badge--highway { background: rgba(107,114,128,0.2); color: #d1d5db; border: 1px solid rgba(107,114,128,0.4); }
  .qro-detail-badge--forestal { background: rgba(22,163,74,0.2); color: #86efac; border: 1px solid rgba(22,163,74,0.4); }

  .qro-detail-hero__title { font-size: clamp(1.75rem, 3.5vw, 2.75rem); font-weight: 800; color: white; line-height: 1.15; margin: 0 0 var(--space-4); }
  .qro-detail-hero__desc { font-size: var(--text-base); color: rgba(255,255,255,0.85); line-height: 1.7; margin: 0 0 var(--space-6); }

  .qro-detail-hero__address { display: flex; align-items: flex-start; gap: var(--space-3); padding: var(--space-4); background: rgba(255,255,255,0.08); border-radius: var(--radius-lg); margin-bottom: var(--space-6); }
  .qro-detail-hero__address svg { flex-shrink: 0; color: var(--qro-primary-light); margin-top: 2px; }
  .qro-detail-hero__address span { font-size: var(--text-sm); color: white; line-height: 1.5; }

  .qro-detail-hero__meta { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-4); padding: var(--space-5); background: rgba(255,255,255,0.06); border-radius: var(--radius-xl); margin-bottom: var(--space-6); }
  .qro-detail-hero__meta-item { display: flex; flex-direction: column; gap: var(--space-1); }
  .qro-detail-hero__meta-label { font-size: var(--text-xs); color: rgba(255,255,255,0.5); text-transform: uppercase; letter-spacing: 0.05em; }
  .qro-detail-hero__meta-value { font-size: var(--text-sm); font-weight: 600; color: white; }

  .qro-detail-bilingual { padding: var(--space-4); background: rgba(255,255,255,0.08); border-radius: var(--radius-lg); border-left: 3px solid var(--qro-primary-light); }
  .qro-detail-bilingual__header { display: flex; align-items: center; gap: var(--space-2); margin-bottom: var(--space-2); }
  .qro-detail-bilingual__flag { padding: var(--space-1) var(--space-2); background: var(--qro-primary); border-radius: var(--radius-sm); font-size: var(--text-xs); font-weight: 700; color: white; }
  .qro-detail-bilingual__header span { font-size: var(--text-sm); color: rgba(255,255,255,0.7); }
  .qro-detail-bilingual p { margin: 0; font-size: var(--text-sm); color: rgba(255,255,255,0.8); line-height: 1.6; }

  /* Emergency Card */
  .qro-detail-emergency { background: rgba(255,255,255,0.12); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.18); border-radius: var(--radius-2xl); padding: var(--space-6); }
  .qro-detail-emergency__main { text-align: center; padding-bottom: var(--space-5); border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: var(--space-5); }
  .qro-detail-emergency__label { display: block; font-size: var(--text-xs); color: rgba(255,255,255,0.6); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: var(--space-2); }
  .qro-detail-emergency__911 { display: block; font-size: 4rem; font-weight: 900; color: var(--color-fire-400); text-decoration: none; line-height: 1; margin-bottom: var(--space-1); text-shadow: 0 0 30px rgba(239, 68, 68, 0.5); }
  .qro-detail-emergency__note { display: block; font-size: var(--text-xs); color: rgba(255,255,255,0.5); }

  .qro-detail-emergency__local { text-align: center; padding-bottom: var(--space-5); border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: var(--space-4); }
  .qro-detail-emergency__local-label { display: block; font-size: var(--text-xs); color: rgba(255,255,255,0.6); margin-bottom: var(--space-2); }
  .qro-detail-emergency__phone { display: block; font-size: var(--text-2xl); font-weight: 700; color: var(--qro-primary-light); text-decoration: none; }
  .qro-detail-emergency__phone:hover { color: white; }

  .qro-detail-emergency__alt { display: flex; justify-content: space-between; padding: var(--space-2); background: rgba(255,255,255,0.05); border-radius: var(--radius-md); margin-bottom: var(--space-4); }
  .qro-detail-emergency__alt span { font-size: var(--text-xs); color: rgba(255,255,255,0.6); }
  .qro-detail-emergency__alt a { font-size: var(--text-xs); font-weight: 600; color: white; text-decoration: none; }

  .qro-detail-emergency__actions { display: flex; flex-direction: column; gap: var(--space-2); }
  .qro-detail-emergency__action { display: flex; align-items: center; justify-content: center; gap: var(--space-2); padding: var(--space-3); background: rgba(255,255,255,0.08); border-radius: var(--radius-lg); text-decoration: none; color: white; font-size: var(--text-sm); font-weight: 500; transition: all 0.2s; }
  .qro-detail-emergency__action:hover { background: rgba(255,255,255,0.15); }
  .qro-detail-emergency__action--directions { background: var(--qro-primary); }
  .qro-detail-emergency__action--directions:hover { background: var(--qro-primary-dark); }

  /* ═══ Section Styles ═══ */
  .qro-detail-section-header { margin-bottom: var(--space-8); }
  .qro-detail-section-header--left { text-align: left; }
  .qro-detail-section-label { display: inline-block; padding: var(--space-1) var(--space-3); background: rgba(14,165,233,0.1); border-radius: var(--radius-full); font-size: var(--text-xs); font-weight: 600; color: var(--qro-primary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: var(--space-3); }
  .qro-detail-section-header h2 { font-size: var(--text-2xl); font-weight: 800; color: var(--color-smoke-900); margin: 0; }

  /* ═══ Services ═══ */
  .qro-detail-services { padding: var(--space-16) 0; background: white; }
  .qro-detail-services__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-4); }
  .qro-detail-service { position: relative; display: flex; align-items: center; gap: var(--space-4); padding: var(--space-5); background: var(--color-smoke-50); border-radius: var(--radius-xl); border: 1px solid var(--color-smoke-200); transition: all 0.2s; }
  .qro-detail-service:hover { background: white; border-color: var(--qro-primary-light); box-shadow: 0 4px 12px rgba(14,165,233,0.1); }
  .qro-detail-service--hazmat { border-left: 3px solid var(--qro-hazmat); }
  .qro-detail-service--rescue { border-left: 3px solid var(--qro-forestal); }
  .qro-detail-service--medical { border-left: 3px solid var(--qro-primary); }
  .qro-detail-service__icon { display: flex; align-items: center; justify-content: center; width: 48px; height: 48px; background: linear-gradient(135deg, var(--qro-primary), var(--qro-primary-dark)); border-radius: var(--radius-lg); color: white; flex-shrink: 0; }
  .qro-detail-service--hazmat .qro-detail-service__icon { background: linear-gradient(135deg, var(--qro-hazmat), #b91c1c); }
  .qro-detail-service--rescue .qro-detail-service__icon { background: linear-gradient(135deg, var(--qro-forestal), #15803d); }
  .qro-detail-service--medical .qro-detail-service__icon { background: linear-gradient(135deg, #3b82f6, #2563eb); }
  .qro-detail-service__name { font-size: var(--text-sm); font-weight: 600; color: var(--color-smoke-800); line-height: 1.4; flex: 1; }
  .qro-detail-service__number { position: absolute; top: var(--space-2); right: var(--space-3); font-size: var(--text-xs); font-weight: 700; color: var(--color-smoke-300); }

  /* ═══ Equipment ═══ */
  .qro-detail-equipment { padding: var(--space-16) 0; background: var(--color-smoke-50); }
  .qro-detail-equipment__grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--space-4); }
  .qro-detail-equipment__item { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-4); background: white; border-radius: var(--radius-lg); border: 1px solid var(--color-smoke-200); }
  .qro-detail-equipment__item--vehicle { border-left: 3px solid var(--qro-primary); }
  .qro-detail-equipment__item--hazmat { border-left: 3px solid var(--qro-hazmat); }
  .qro-detail-equipment__item--rescue { border-left: 3px solid var(--qro-forestal); }
  .qro-detail-equipment__icon { display: flex; align-items: center; justify-content: center; width: 40px; height: 40px; background: var(--color-smoke-100); border-radius: var(--radius-md); color: var(--color-smoke-600); flex-shrink: 0; }
  .qro-detail-equipment__item--vehicle .qro-detail-equipment__icon { background: rgba(14,165,233,0.1); color: var(--qro-primary); }
  .qro-detail-equipment__item--hazmat .qro-detail-equipment__icon { background: rgba(220,38,38,0.1); color: var(--qro-hazmat); }
  .qro-detail-equipment__item--rescue .qro-detail-equipment__icon { background: rgba(22,163,74,0.1); color: var(--qro-forestal); }
  .qro-detail-equipment__name { font-size: var(--text-sm); font-weight: 500; color: var(--color-smoke-700); line-height: 1.4; }

  /* ═══ Aerospace Section ═══ */
  .qro-detail-aerospace { padding: var(--space-16) 0; background: linear-gradient(180deg, #f0f9ff 0%, white 100%); }
  .qro-detail-aerospace__grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--space-4); margin-bottom: var(--space-10); }
  .qro-detail-aerospace__card { padding: var(--space-5); background: white; border-radius: var(--radius-xl); border: 1px solid var(--color-smoke-200); box-shadow: 0 2px 8px rgba(0,0,0,0.03); }
  .qro-detail-aerospace__card h3 { font-size: var(--text-base); font-weight: 700; color: var(--color-smoke-900); margin: 0 0 var(--space-1); }
  .qro-detail-aerospace__location { font-size: var(--text-sm); color: var(--qro-primary); margin: 0 0 var(--space-3); }
  .qro-detail-aerospace__details { display: flex; flex-direction: column; gap: var(--space-1); font-size: var(--text-sm); color: var(--color-smoke-600); margin-bottom: var(--space-3); }
  .qro-detail-aerospace__risk { font-size: var(--text-xs); padding: var(--space-2); background: rgba(220,38,38,0.08); border-radius: var(--radius-md); color: var(--qro-hazmat); }
  .qro-detail-aerospace__certifications { padding: var(--space-6); background: linear-gradient(135deg, var(--qro-primary), var(--qro-primary-dark)); border-radius: var(--radius-xl); }
  .qro-detail-aerospace__certifications h4 { font-size: var(--text-base); font-weight: 700; color: white; margin: 0 0 var(--space-4); text-align: center; }
  .qro-detail-aerospace__cert-grid { display: flex; justify-content: center; gap: var(--space-3); flex-wrap: wrap; }
  .qro-detail-aerospace__cert { padding: var(--space-2) var(--space-4); background: rgba(255,255,255,0.2); border-radius: var(--radius-full); font-size: var(--text-sm); font-weight: 600; color: white; }

  /* ═══ Coverage Section ═══ */
  .qro-detail-coverage { padding: var(--space-16) 0; background: white; }
  .qro-detail-coverage__grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-10); }
  .qro-detail-coverage__zone { padding: var(--space-5); background: var(--color-smoke-50); border-radius: var(--radius-xl); margin-bottom: var(--space-6); }
  .qro-detail-coverage__zone h3 { font-size: var(--text-lg); font-weight: 700; color: var(--color-smoke-900); margin: 0 0 var(--space-2); padding-left: var(--space-4); border-left: 3px solid; }
  .qro-detail-coverage__zone p { font-size: var(--text-sm); color: var(--color-smoke-600); margin: 0 0 var(--space-4); line-height: 1.6; }
  .qro-detail-coverage__characteristics { display: flex; flex-wrap: wrap; gap: var(--space-2); }
  .qro-detail-coverage__char { padding: var(--space-1) var(--space-3); background: white; border-radius: var(--radius-full); font-size: var(--text-xs); color: var(--color-smoke-600); border: 1px solid var(--color-smoke-200); }
  .qro-detail-coverage__municipalities h4 { font-size: var(--text-base); font-weight: 700; color: var(--color-smoke-800); margin: 0 0 var(--space-3); }
  .qro-detail-coverage__municipalities ul { list-style: none; padding: 0; margin: 0 0 var(--space-6); display: grid; gap: var(--space-2); }
  .qro-detail-coverage__municipalities li { font-size: var(--text-sm); color: var(--color-smoke-600); padding-left: var(--space-4); position: relative; }
  .qro-detail-coverage__municipalities li::before { content: ""; position: absolute; left: 0; top: 8px; width: 6px; height: 6px; background: var(--qro-primary); border-radius: 50%; }
  .qro-detail-coverage__risks h4 { font-size: var(--text-base); font-weight: 700; color: var(--color-smoke-800); margin: 0 0 var(--space-3); }
  .qro-detail-coverage__risks-list { display: flex; flex-wrap: wrap; gap: var(--space-2); }
  .qro-detail-coverage__risk-tag { padding: var(--space-1) var(--space-3); background: rgba(220,38,38,0.08); border-radius: var(--radius-full); font-size: var(--text-xs); font-weight: 500; color: var(--qro-hazmat); }

  .qro-detail-map { position: relative; }
  .qro-detail-map iframe { border-radius: var(--radius-xl); box-shadow: 0 8px 30px rgba(0,0,0,0.1); }
  .qro-detail-map__coords { display: flex; align-items: center; gap: var(--space-2); margin-top: var(--space-3); font-size: var(--text-sm); color: var(--color-smoke-500); }
  .qro-detail-map__coords code { padding: var(--space-1) var(--space-2); background: var(--color-smoke-100); border-radius: var(--radius-sm); font-family: monospace; font-size: var(--text-xs); }

  /* ═══ Features ═══ */
  .qro-detail-features { padding: var(--space-16) 0; background: var(--color-smoke-50); }
  .qro-detail-features__grid { display: flex; flex-wrap: wrap; justify-content: center; gap: var(--space-4); }
  .qro-detail-feature { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-4) var(--space-6); background: white; border-radius: var(--radius-full); border: 1px solid var(--color-smoke-200); box-shadow: 0 2px 8px rgba(0,0,0,0.03); }
  .qro-detail-feature__icon { font-size: var(--text-xl); }
  .qro-detail-feature__text { font-size: var(--text-sm); font-weight: 600; color: var(--color-smoke-700); }

  /* ═══ FAQ ═══ */
  .qro-detail-faq { padding: var(--space-16) 0; background: white; }
  .qro-detail-faq__grid { display: grid; gap: var(--space-4); max-width: 800px; margin: 0 auto; }
  .qro-detail-faq__item { padding: var(--space-5); background: var(--color-smoke-50); border-radius: var(--radius-xl); border: 1px solid var(--color-smoke-200); }
  .qro-detail-faq__item[open] { background: white; border-color: var(--qro-primary-light); }
  .qro-detail-faq__item summary { font-size: var(--text-base); font-weight: 600; color: var(--color-smoke-900); cursor: pointer; list-style: none; }
  .qro-detail-faq__item summary::-webkit-details-marker { display: none; }
  .qro-detail-faq__item p { font-size: var(--text-sm); color: var(--color-smoke-600); line-height: 1.7; margin: var(--space-4) 0 0; padding-top: var(--space-4); border-top: 1px solid var(--color-smoke-200); }

  /* ═══ Related ═══ */
  .qro-detail-related { padding: var(--space-16) 0; background: var(--color-smoke-50); }
  .qro-detail-related__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-6); margin-bottom: var(--space-8); }
  .qro-detail-related__card { display: block; padding: var(--space-6); background: white; border-radius: var(--radius-xl); text-decoration: none; border: 1px solid var(--color-smoke-200); transition: all 0.2s; }
  .qro-detail-related__card:hover { border-color: var(--qro-primary); box-shadow: 0 8px 24px rgba(14,165,233,0.1); transform: translateY(-2px); }
  .qro-detail-related__card h3 { font-size: var(--text-lg); font-weight: 700; color: var(--color-smoke-900); margin: 0 0 var(--space-2); }
  .qro-detail-related__card p { font-size: var(--text-sm); color: var(--color-smoke-500); margin: 0 0 var(--space-4); line-height: 1.5; }
  .qro-detail-related__meta { display: flex; justify-content: space-between; align-items: center; }
  .qro-detail-related__phone { font-size: var(--text-sm); font-weight: 600; color: var(--qro-primary); }
  .qro-detail-related__meta span:last-child { font-size: var(--text-xs); color: var(--color-smoke-400); }
  .qro-detail-related__empty { text-align: center; padding: var(--space-8); color: var(--color-smoke-500); }
  .qro-detail-related__all { text-align: center; }

  .qro-detail-btn { display: inline-flex; align-items: center; gap: var(--space-2); padding: var(--space-3) var(--space-6); border-radius: var(--radius-lg); font-size: var(--text-sm); font-weight: 600; text-decoration: none; transition: all 0.2s; }
  .qro-detail-btn--outline { background: white; border: 2px solid var(--qro-primary); color: var(--qro-primary); }
  .qro-detail-btn--outline:hover { background: var(--qro-primary); color: white; }

  /* ═══ Interlinking ═══ */
  .qro-detail-interlinking { padding: var(--space-16) 0; background: white; }
  .qro-detail-interlinking__grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-10); }
  .qro-detail-interlinking h3 { font-size: var(--text-lg); font-weight: 700; color: var(--color-smoke-900); margin: 0 0 var(--space-4); }
  .qro-detail-interlinking__states-grid, .qro-detail-interlinking__products-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-3); }
  .qro-detail-neighbor-link, .qro-detail-product-link { display: flex; flex-direction: column; padding: var(--space-4); background: var(--color-smoke-50); border-radius: var(--radius-lg); text-decoration: none; border: 1px solid var(--color-smoke-200); transition: all 0.2s; }
  .qro-detail-neighbor-link:hover, .qro-detail-product-link:hover { background: white; border-color: var(--qro-primary-light); }
  .qro-detail-neighbor-link__name, .qro-detail-product-link__name { font-size: var(--text-sm); font-weight: 600; color: var(--color-smoke-800); margin-bottom: var(--space-1); }
  .qro-detail-neighbor-link__context, .qro-detail-product-link__context { font-size: var(--text-xs); color: var(--color-smoke-500); }

  /* ═══ CTA ═══ */
  .qro-detail-cta { padding: var(--space-16) 0; background: linear-gradient(135deg, #0c4a6e 0%, #075985 50%, var(--qro-primary) 100%); }
  .qro-detail-cta__content { text-align: center; max-width: 600px; margin: 0 auto; }
  .qro-detail-cta__content h2 { font-size: var(--text-2xl); font-weight: 800; color: white; margin: 0 0 var(--space-4); }
  .qro-detail-cta__content p { font-size: var(--text-base); color: rgba(255,255,255,0.85); margin: 0 0 var(--space-8); line-height: 1.7; }
  .qro-detail-cta__btn { display: inline-flex; align-items: center; gap: var(--space-2); padding: var(--space-4) var(--space-8); background: white; border-radius: var(--radius-lg); font-size: var(--text-base); font-weight: 700; color: var(--qro-primary); text-decoration: none; transition: all 0.2s; }
  .qro-detail-cta__btn:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.2); }

  /* ═══ Responsive ═══ */
  @media (max-width: 1200px) {
    .qro-detail-services__grid { grid-template-columns: repeat(2, 1fr); }
    .qro-detail-equipment__grid { grid-template-columns: repeat(3, 1fr); }
    .qro-detail-aerospace__grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 1024px) {
    .qro-detail-hero__grid { grid-template-columns: 1fr; }
    .qro-detail-emergency { max-width: 400px; }
    .qro-detail-coverage__grid { grid-template-columns: 1fr; }
    .qro-detail-related__grid { grid-template-columns: repeat(2, 1fr); }
    .qro-detail-interlinking__grid { grid-template-columns: 1fr; gap: var(--space-8); }
  }

  @media (max-width: 768px) {
    .qro-detail-hero { padding: var(--space-10) 0 var(--space-12); }
    .qro-detail-services__grid, .qro-detail-equipment__grid, .qro-detail-related__grid { grid-template-columns: 1fr; }
    .qro-detail-aerospace__grid { grid-template-columns: 1fr; }
    .qro-detail-hero__meta { grid-template-columns: 1fr; }
    .qro-detail-interlinking__states-grid, .qro-detail-interlinking__products-grid { grid-template-columns: 1fr; }
  }
</style>
