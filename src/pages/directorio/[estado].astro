---
/**
 * [estado].astro
 * ──────────────────────────────────────────────────────────────────
 * ⚠️  ARCHIVO INERTE — NO GENERA RUTAS.
 *
 * Antes era una página dinámica que listaba 20 estados como "pendientes"
 * con noindex, pero esos 20 estados ya tienen archivo .astro estático
 * (jalisco.astro, yucatan.astro, etc.) que toma precedencia en Astro.
 *
 * Para mantener trazabilidad histórica y permitir restauración rápida,
 * conservamos el archivo con getStaticPaths() devolviendo un array vacío.
 * Resultado: 0 rutas generadas, 0 código muerto activo.
 *
 * Reemplaza al placeholder original (138 líneas eliminadas).
 * Si decides reactivarlo, hazlo SOLO para slugs que NO tengan .astro estático.
 * ──────────────────────────────────────────────────────────────────
 */

export function getStaticPaths() {
  return [];
}

const { estado } = Astro.params as { estado: string };
---

<!-- Esta plantilla no se renderiza porque getStaticPaths() devuelve []. -->
<p>Estado: {estado}</p>
