---
/**
 * MICHOACÁN - Páginas Dinámicas de Estaciones de Bomberos
 * Contenido SEO profesional con contexto UNESCO y regional
 */
import PageLayout from "@layouts/PageLayout.astro";
import {
  estacionesMichoacan,
  MICHOACAN_SEO,
  generarMetaSEO,
  getContextoZona
} from '../../../data/estaciones-michoacan';

export function getStaticPaths() {
  return estacionesMichoacan.map((estacion) => ({
    params: { slug: estacion.slug },
    props: { estacion }
  }));
}

const { estacion } = Astro.props;
const metaSEO = generarMetaSEO(estacion);
const contextoZona = estacion.zona ? getContextoZona(estacion.zona) : null;

// Schema.org
const stationSchema = {
  "@context": "https://schema.org",
  "@type": "FireStation",
  "name": estacion.nombre,
  "description": `Estación de bomberos en ${estacion.ciudad}, Michoacán. ${estacion.servicios.slice(0, 3).join(", ")}.`,
  "address": {
    "@type": "PostalAddress",
    "streetAddress": estacion.direccion,
    "addressLocality": estacion.ciudad,
    "addressRegion": "Michoacán",
    "postalCode": estacion.cp,
    "addressCountry": "MX"
  },
  "telephone": estacion.telefono,
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"],
    "opens": "00:00",
    "closes": "23:59"
  }
};

// Contextos especiales
const esMorelia = estacion.ciudad === "Morelia";
const esPatzcuaro = estacion.ciudad === "Pátzcuaro" || estacion.slug.includes("patzcuaro");
const esUruapan = estacion.ciudad === "Uruapan" || estacion.slug.includes("uruapan");
const esLazaro = estacion.ciudad === "Lázaro Cárdenas" || estacion.slug.includes("lazaro");
const esMariposa = estacion.zona === "Región Oriente";
const esCosta = estacion.zona === "Costa del Pacífico";
const esZamora = estacion.ciudad === "Zamora de Hidalgo" || estacion.slug.includes("zamora");
---

<PageLayout title={metaSEO.title} description={metaSEO.description}>
  <script type="application/ld+json" set:html={JSON.stringify(stationSchema)} />

  <div class="station-page">
    <!-- Hero -->
    <section class="station-hero">
      <div class="hero-overlay"></div>
      <div class="hero-content">
        <nav class="breadcrumb">
          <a href="/">Inicio</a>
          <span>/</span>
          <a href="/directorio">Directorio</a>
          <span>/</span>
          <a href="/directorio/michoacan">Michoacán</a>
          <span>/</span>
          <span class="current">{estacion.ciudad}</span>
        </nav>

        <div class="hero-badges">
          {estacion.zona && (
            <span class="badge zone">{estacion.zona}</span>
          )}
          {estacion.especialidades?.map((esp) => (
            <span class="badge specialty" data-type={
              esp.toLowerCase().includes("hazmat") ? "hazmat" :
              esp.toLowerCase().includes("patrimoni") || esp.toLowerCase().includes("unesco") ? "unesco" :
              esp.toLowerCase().includes("acuático") ? "aquatic" :
              esp.toLowerCase().includes("forestal") || esp.toLowerCase().includes("biosfera") ? "forest" :
              esp.toLowerCase().includes("mariposa") ? "monarch" :
              esp.toLowerCase().includes("industrial") ? "industrial" : "default"
            }>
              {esp}
            </span>
          ))}
        </div>

        <h1>{estacion.nombre}</h1>
        <p class="hero-location">
          <span class="icon">📍</span>
          {estacion.direccion}, {estacion.ciudad}, Michoacán C.P. {estacion.cp}
        </p>

        <div class="hero-phones">
          <a href={`tel:${estacion.telefono.replace(/\D/g, '')}`} class="phone-main">
            <span class="phone-icon">📞</span>
            <span class="phone-number">{estacion.telefono}</span>
          </a>
          {estacion.telefono2 && (
            <a href={`tel:${estacion.telefono2.replace(/\D/g, '')}`} class="phone-secondary">
              {estacion.telefono2}
            </a>
          )}
        </div>

        <div class="hero-status">
          <span class="status-badge open">
            <span class="pulse"></span>
            Abierto 24 horas
          </span>
          {estacion.tiempoRespuesta && (
            <span class="response-time">⚡ {estacion.tiempoRespuesta}</span>
          )}
        </div>
      </div>
    </section>

    <!-- Alertas contextuales -->
    {esMorelia && (
      <div class="context-alert morelia">
        <span class="alert-icon">🏛️</span>
        <div class="alert-text">
          <strong>Centro Histórico de Morelia - Patrimonio UNESCO</strong>
          <span>Protección especializada de arquitectura colonial en cantera rosa, acueducto del siglo XVIII y edificios virreinales.</span>
        </div>
      </div>
    )}

    {esPatzcuaro && (
      <div class="context-alert patzcuaro">
        <span class="alert-icon">💀</span>
        <div class="alert-text">
          <strong>Pátzcuaro - Corazón del Día de Muertos</strong>
          <span>Rescate acuático en Lago de Pátzcuaro, emergencias en Isla de Janitzio y atención masiva durante celebraciones de noviembre.</span>
        </div>
      </div>
    )}

    {esUruapan && (
      <div class="context-alert uruapan">
        <span class="alert-icon">🥑</span>
        <div class="alert-text">
          <strong>Uruapan - Capital Mundial del Aguacate</strong>
          <span>Emergencias agroindustriales, incendios en empacadoras y manejo de materiales peligrosos agroquímicos.</span>
        </div>
      </div>
    )}

    {esLazaro && (
      <div class="context-alert lazaro">
        <span class="alert-icon">🏭</span>
        <div class="alert-text">
          <strong>Puerto Lázaro Cárdenas - Principal del Pacífico</strong>
          <span>HAZMAT Nivel III, emergencias portuarias, incendios industriales y coordinación con siderúrgica ArcelorMittal.</span>
        </div>
      </div>
    )}

    {esMariposa && (
      <div class="context-alert mariposa">
        <span class="alert-icon">🦋</span>
        <div class="alert-text">
          <strong>Reserva de la Biosfera Mariposa Monarca - UNESCO</strong>
          <span>Protección de santuarios de hibernación, prevención de incendios forestales en bosques de oyamel.</span>
        </div>
      </div>
    )}

    {esZamora && (
      <div class="context-alert zamora">
        <span class="alert-icon">🍓</span>
        <div class="alert-text">
          <strong>Zamora - Capital Nacional de la Fresa</strong>
          <span>Emergencias en invernaderos, incendios agroindustriales y manejo de agroquímicos en zona de berries.</span>
        </div>
      </div>
    )}

    <!-- Main Content -->
    <div class="main-container">
      <div class="content-grid">
        <main class="station-main">
          <!-- Servicios -->
          <section class="info-card services">
            <h2>🚒 Servicios de Emergencia</h2>
            <ul class="services-list">
              {estacion.servicios.map((servicio) => (
                <li>
                  <span class="service-icon">✓</span>
                  {servicio}
                </li>
              ))}
            </ul>
          </section>

          <!-- Especialidades -->
          {estacion.especialidades && estacion.especialidades.length > 0 && (
            <section class="info-card specialties-section">
              <h2>🎯 Especialidades</h2>
              <div class="specialties-grid">
                {estacion.especialidades.map((esp) => (
                  <div class="specialty-card" data-type={
                    esp.toLowerCase().includes("hazmat") ? "hazmat" :
                    esp.toLowerCase().includes("patrimoni") || esp.toLowerCase().includes("unesco") ? "unesco" :
                    esp.toLowerCase().includes("acuático") || esp.toLowerCase().includes("lacustre") ? "aquatic" :
                    esp.toLowerCase().includes("forestal") || esp.toLowerCase().includes("biosfera") ? "forest" :
                    esp.toLowerCase().includes("mariposa") || esp.toLowerCase().includes("monarca") ? "monarch" :
                    esp.toLowerCase().includes("industrial") || esp.toLowerCase().includes("portuaria") ? "industrial" :
                    esp.toLowerCase().includes("aguacate") || esp.toLowerCase().includes("agrícola") ? "agro" : "default"
                  }>
                    <span class="specialty-icon">
                      {esp.toLowerCase().includes("hazmat") ? "☣️" :
                       esp.toLowerCase().includes("patrimoni") || esp.toLowerCase().includes("unesco") ? "🏛️" :
                       esp.toLowerCase().includes("acuático") || esp.toLowerCase().includes("lacustre") ? "🚣" :
                       esp.toLowerCase().includes("forestal") ? "🌲" :
                       esp.toLowerCase().includes("mariposa") || esp.toLowerCase().includes("biosfera") ? "🦋" :
                       esp.toLowerCase().includes("industrial") || esp.toLowerCase().includes("portuaria") ? "🏭" :
                       esp.toLowerCase().includes("aguacate") || esp.toLowerCase().includes("agro") ? "🥑" :
                       esp.toLowerCase().includes("rescate") ? "⛑️" :
                       esp.toLowerCase().includes("masivo") ? "👥" : "🔧"}
                    </span>
                    <span class="specialty-name">{esp}</span>
                  </div>
                ))}
              </div>
            </section>
          )}

          <!-- Cobertura -->
          {estacion.cobertura && estacion.cobertura.length > 0 && (
            <section class="info-card coverage">
              <h2>🗺️ Área de Cobertura</h2>
              <div class="coverage-tags">
                {estacion.cobertura.map((area) => (
                  <span class="coverage-tag">{area}</span>
                ))}
              </div>
            </section>
          )}

          <!-- Historia -->
          {estacion.historia && (
            <section class="info-card history">
              <h2>📜 Historia</h2>
              <p>{estacion.historia}</p>
            </section>
          )}

          <!-- Contexto de Zona -->
          {contextoZona && (
            <section class="info-card zone-context">
              <h2>📍 Contexto Regional: {estacion.zona}</h2>
              <p class="zone-description">{contextoZona.descripcion}</p>

              <div class="zone-details">
                <div class="zone-risks">
                  <h3>⚠️ Riesgos Principales</h3>
                  <ul>
                    {contextoZona.riesgos.map((riesgo) => (
                      <li>{riesgo}</li>
                    ))}
                  </ul>
                </div>
                <div class="zone-features">
                  <h3>🏛️ Características</h3>
                  <ul>
                    {contextoZona.caracteristicas.map((caract) => (
                      <li>{caract}</li>
                    ))}
                  </ul>
                </div>
              </div>
            </section>
          )}

          <!-- Contenido especial por ciudad -->
          {estacion.slug === "estacion-central-morelia" && (
            <section class="info-card special-content morelia-special">
              <h2>🏛️ Patrimonio de la Humanidad UNESCO</h2>
              <p>
                El Centro Histórico de Morelia fue inscrito en la Lista del Patrimonio Mundial de la UNESCO
                en <strong>1991</strong>. Los bomberos de la capital michoacana protegen más de
                <strong>1,000 edificios históricos</strong> de arquitectura colonial en cantera rosa,
                incluyendo la Catedral de Morelia, el Acueducto del siglo XVIII y el Palacio Clavijero.
              </p>
              <div class="special-stats">
                <div class="stat">
                  <span class="stat-icon">🏛️</span>
                  <span class="stat-value">UNESCO 1991</span>
                </div>
                <div class="stat">
                  <span class="stat-icon">🏰</span>
                  <span class="stat-value">1,000+ edificios</span>
                </div>
                <div class="stat">
                  <span class="stat-icon">🪨</span>
                  <span class="stat-value">Cantera rosa</span>
                </div>
              </div>
            </section>
          )}

          {estacion.slug === "estacion-bomberos-patzcuaro" && (
            <section class="info-card special-content patzcuaro-special">
              <h2>💀 Día de Muertos - Tradición Viva</h2>
              <p>
                Pátzcuaro es el epicentro del <strong>Día de Muertos más tradicional de México</strong>.
                Cada 1 y 2 de noviembre, cientos de miles de visitantes llegan a presenciar las ceremonias
                en la <strong>Isla de Janitzio</strong>. Los bomberos mantienen brigadas de rescate acuático
                y atención de emergencias por aglomeraciones durante toda la temporada.
              </p>
              <div class="muertos-info">
                <span class="muertos-badge">Patrimonio Inmaterial</span>
                <ul>
                  <li>🚣 Rescate acuático lacustre activo</li>
                  <li>👥 Protocolos de eventos masivos</li>
                  <li>🏥 Coordinación con servicios médicos</li>
                  <li>🌙 Vigilancia nocturna 1-2 noviembre</li>
                </ul>
              </div>
            </section>
          )}

          {estacion.slug === "estacion-central-uruapan" && (
            <section class="info-card special-content aguacate-special">
              <h2>🥑 Capital Mundial del Aguacate</h2>
              <p>
                Uruapan produce más aguacate que cualquier otra región del mundo. Michoacán aporta el
                <strong>80% de la producción nacional</strong> y es el principal exportador a Estados Unidos.
                Los bomberos están especializados en emergencias en <strong>más de 100 empacadoras</strong>,
                incendios en huertas y manejo de materiales peligrosos agroquímicos.
              </p>
              <div class="aguacate-stats">
                <div class="stat">
                  <span class="stat-icon">🥑</span>
                  <span class="stat-value">80% nacional</span>
                </div>
                <div class="stat">
                  <span class="stat-icon">🏭</span>
                  <span class="stat-value">100+ empacadoras</span>
                </div>
                <div class="stat">
                  <span class="stat-icon">☣️</span>
                  <span class="stat-value">HAZMAT agro</span>
                </div>
              </div>
            </section>
          )}

          {estacion.slug === "estacion-central-lazaro-cardenas" && (
            <section class="info-card special-content puerto-special">
              <h2>🏭 Puerto Industrial del Pacífico</h2>
              <p>
                El Puerto de Lázaro Cárdenas es el <strong>más importante del Pacífico mexicano</strong>,
                manejando millones de contenedores anuales. La estación cuenta con certificación
                <strong>HAZMAT Nivel III</strong> y coordina con la siderúrgica ArcelorMittal,
                una de las más grandes del país.
              </p>
              <div class="puerto-capabilities">
                <h3>Capacidades Especiales</h3>
                <ul>
                  <li>☢️ HAZMAT Nivel III certificado</li>
                  <li>🚢 Emergencias portuarias y buques</li>
                  <li>⚙️ Incendios siderúrgicos</li>
                  <li>📦 Incidentes con contenedores</li>
                  <li>🌊 Rescate marítimo costero</li>
                </ul>
              </div>
            </section>
          )}

          {esMariposa && estacion.slug.includes("zitacuaro") && (
            <section class="info-card special-content mariposa-special">
              <h2>🦋 Reserva de la Biosfera Mariposa Monarca</h2>
              <p>
                Declarada Patrimonio Natural de la Humanidad por UNESCO en <strong>2008</strong>,
                la Reserva alberga los santuarios donde <strong>millones de mariposas monarca</strong>
                hibernan cada año entre noviembre y marzo. Los bomberos protegen los bosques de oyamel
                con brigadas especializadas en prevención y combate de incendios forestales.
              </p>
              <div class="mariposa-info">
                <span class="unesco-badge">🏆 UNESCO 2008</span>
                <div class="mariposa-stats">
                  <div class="stat">
                    <span class="stat-icon">🦋</span>
                    <span class="stat-value">Millones de mariposas</span>
                  </div>
                  <div class="stat">
                    <span class="stat-icon">🌲</span>
                    <span class="stat-value">Bosques de oyamel</span>
                  </div>
                  <div class="stat">
                    <span class="stat-icon">🔥</span>
                    <span class="stat-value">Brigadas forestales</span>
                  </div>
                </div>
              </div>
            </section>
          )}
        </main>

        <!-- Sidebar -->
        <aside class="station-sidebar">
          <!-- Contacto -->
          <div class="sidebar-card contact-card">
            <h3>📞 Contacto de Emergencia</h3>
            <a href={`tel:${estacion.telefono.replace(/\D/g, '')}`} class="contact-phone main">
              <span class="phone-icon">🚒</span>
              <span class="phone-info">
                <strong>{estacion.telefono}</strong>
                <small>Línea principal</small>
              </span>
            </a>
            {estacion.telefono2 && (
              <a href={`tel:${estacion.telefono2.replace(/\D/g, '')}`} class="contact-phone secondary">
                <span class="phone-icon">📞</span>
                <span class="phone-info">
                  <strong>{estacion.telefono2}</strong>
                  <small>Línea secundaria</small>
                </span>
              </a>
            )}
            <a href="tel:911" class="contact-phone emergency">
              <span class="phone-icon">🆘</span>
              <span class="phone-info">
                <strong>911</strong>
                <small>Emergencias Nacional</small>
              </span>
            </a>
          </div>

          <!-- Ubicación -->
          <div class="sidebar-card location-card">
            <h3>📍 Ubicación</h3>
            <address>
              {estacion.direccion}<br />
              {estacion.ciudad}, Michoacán<br />
              C.P. {estacion.cp}
            </address>
            {estacion.coordenadas && (
              <a
                href={`https://www.google.com/maps?q=${estacion.coordenadas.lat},${estacion.coordenadas.lng}`}
                target="_blank"
                rel="noopener noreferrer"
                class="map-link"
              >
                Ver en Google Maps →
              </a>
            )}
          </div>

          <!-- Horario -->
          <div class="sidebar-card hours-card">
            <h3>🕐 Horario de Servicio</h3>
            <div class="hours-status">
              <span class="status-dot"></span>
              <span class="status-text">Operativo 24/7</span>
            </div>
            <p class="hours-detail">{estacion.horario}</p>
            {estacion.tiempoRespuesta && (
              <div class="response-info">
                <span class="response-icon">⚡</span>
                <span>Respuesta: {estacion.tiempoRespuesta}</span>
              </div>
            )}
          </div>

          <!-- Comandancia -->
          {estacion.comandancia && (
            <div class="sidebar-card command-card">
              <h3>🏛️ Comandancia</h3>
              <p>{estacion.comandancia}</p>
            </div>
          )}

          <!-- Info Estado -->
          <div class="sidebar-card state-card">
            <h3>🟣 Michoacán</h3>
            <ul class="state-info">
              <li><strong>Capital:</strong> {MICHOACAN_SEO.capital}</li>
              <li><strong>Población:</strong> {MICHOACAN_SEO.poblacion}</li>
              <li><strong>Municipios:</strong> {MICHOACAN_SEO.municipios}</li>
              <li><strong>Patrimonio:</strong> 2 sitios UNESCO</li>
            </ul>
            <a href="/directorio/michoacan" class="back-link">
              ← Ver todas las estaciones
            </a>
          </div>
        </aside>
      </div>
    </div>
  </div>
</PageLayout>

<style>
  /* Variables Michoacán */
  :root {
    --mich-primary: #7c3aed;
    --mich-secondary: #a855f7;
    --mich-accent: #f59e0b;
    --mich-dark: #1e1b4b;
    --mich-light: #faf5ff;
    --mich-gradient: linear-gradient(135deg, #7c3aed 0%, #a855f7 50%, #f59e0b 100%);
  }

  .station-page {
    min-height: 100vh;
    background: var(--mich-light);
  }

  /* Hero */
  .station-hero {
    position: relative;
    background: var(--mich-gradient);
    padding: 3rem 1.5rem 4rem;
    color: white;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    pointer-events: none;
  }

  .hero-content {
    max-width: 900px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .breadcrumb {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
    opacity: 0.9;
  }

  .breadcrumb a { color: white; text-decoration: none; }
  .breadcrumb a:hover { text-decoration: underline; }

  .hero-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .badge {
    padding: 0.35rem 0.75rem;
    border-radius: 2rem;
    font-size: 0.8rem;
    font-weight: 600;
  }

  .badge.zone {
    background: rgba(255,255,255,0.2);
    backdrop-filter: blur(10px);
  }

  .badge.specialty[data-type="hazmat"] { background: linear-gradient(135deg, #fbbf24, #f59e0b); color: #78350f; }
  .badge.specialty[data-type="unesco"] { background: linear-gradient(135deg, #c084fc, #a855f7); }
  .badge.specialty[data-type="aquatic"] { background: linear-gradient(135deg, #22d3ee, #06b6d4); color: #164e63; }
  .badge.specialty[data-type="forest"],
  .badge.specialty[data-type="monarch"] { background: linear-gradient(135deg, #4ade80, #22c55e); color: #14532d; }
  .badge.specialty[data-type="industrial"] { background: linear-gradient(135deg, #60a5fa, #3b82f6); }
  .badge.specialty[data-type="default"] { background: rgba(255,255,255,0.25); }

  .station-hero h1 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 800;
    margin-bottom: 0.75rem;
    line-height: 1.2;
  }

  .hero-location {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    font-size: 1rem;
    opacity: 0.95;
    margin-bottom: 1.5rem;
  }

  .hero-phones {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .phone-main {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: white;
    color: var(--mich-primary);
    padding: 0.75rem 1.25rem;
    border-radius: 0.75rem;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.1rem;
  }

  .phone-secondary {
    display: flex;
    align-items: center;
    background: rgba(255,255,255,0.2);
    padding: 0.75rem 1.25rem;
    border-radius: 0.75rem;
    color: white;
    text-decoration: none;
    font-weight: 600;
  }

  .hero-status {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: center;
  }

  .status-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: #16a34a;
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .pulse {
    width: 8px;
    height: 8px;
    background: white;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.2); }
  }

  .response-time {
    background: rgba(255,255,255,0.2);
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-size: 0.9rem;
  }

  /* Context Alerts */
  .context-alert {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    color: white;
  }

  .context-alert.morelia { background: linear-gradient(90deg, #7c3aed, #a855f7); }
  .context-alert.patzcuaro { background: linear-gradient(90deg, #f59e0b, #fbbf24); color: #78350f; }
  .context-alert.uruapan { background: linear-gradient(90deg, #16a34a, #22c55e); }
  .context-alert.lazaro { background: linear-gradient(90deg, #1e40af, #3b82f6); }
  .context-alert.mariposa { background: linear-gradient(90deg, #ea580c, #f59e0b); }
  .context-alert.zamora { background: linear-gradient(90deg, #db2777, #ec4899); }

  .alert-icon { font-size: 1.75rem; flex-shrink: 0; }
  .alert-text { display: flex; flex-direction: column; gap: 0.25rem; }
  .alert-text strong { font-size: 1rem; }
  .alert-text span { font-size: 0.9rem; opacity: 0.9; line-height: 1.5; }

  /* Main Container */
  .main-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 1024px) {
    .content-grid {
      grid-template-columns: 1fr 320px;
    }
  }

  /* Info Cards */
  .info-card {
    background: white;
    border-radius: 1rem;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06);
  }

  .info-card h2 {
    font-size: 1.25rem;
    color: var(--mich-dark);
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid #e2e8f0;
  }

  /* Services */
  .services-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.75rem;
  }

  .services-list li {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 0.75rem;
    background: #f8fafc;
    border-radius: 0.5rem;
    font-size: 0.95rem;
    color: #334155;
  }

  .service-icon {
    color: #16a34a;
    font-weight: 700;
  }

  /* Specialties */
  .specialties-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 1rem;
  }

  .specialty-card {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem;
    border-radius: 0.75rem;
    background: #f1f5f9;
    transition: transform 0.2s;
  }

  .specialty-card:hover { transform: translateY(-2px); }

  .specialty-card[data-type="hazmat"] { background: linear-gradient(135deg, #fef3c7, #fde68a); border: 1px solid #f59e0b; }
  .specialty-card[data-type="unesco"] { background: linear-gradient(135deg, #f3e8ff, #e9d5ff); border: 1px solid #a855f7; }
  .specialty-card[data-type="aquatic"] { background: linear-gradient(135deg, #cffafe, #a5f3fc); border: 1px solid #06b6d4; }
  .specialty-card[data-type="forest"],
  .specialty-card[data-type="monarch"] { background: linear-gradient(135deg, #dcfce7, #bbf7d0); border: 1px solid #22c55e; }
  .specialty-card[data-type="industrial"] { background: linear-gradient(135deg, #dbeafe, #bfdbfe); border: 1px solid #3b82f6; }
  .specialty-card[data-type="agro"] { background: linear-gradient(135deg, #d1fae5, #a7f3d0); border: 1px solid #10b981; }

  .specialty-icon { font-size: 1.5rem; }
  .specialty-name { font-weight: 600; color: #334155; font-size: 0.9rem; }

  /* Coverage */
  .coverage-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .coverage-tag {
    background: linear-gradient(135deg, #f3e8ff, #e9d5ff);
    color: #7c3aed;
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-size: 0.85rem;
    font-weight: 500;
  }

  /* History */
  .history p {
    color: #475569;
    line-height: 1.7;
  }

  /* Zone Context */
  .zone-description {
    color: #475569;
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .zone-details {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5rem;
  }

  .zone-risks, .zone-features {
    padding: 1rem;
    border-radius: 0.75rem;
  }

  .zone-risks { background: #fef2f2; border: 1px solid #fecaca; }
  .zone-features { background: #f3e8ff; border: 1px solid #d8b4fe; }

  .zone-risks h3, .zone-features h3 {
    font-size: 0.95rem;
    margin-bottom: 0.75rem;
    color: var(--mich-dark);
  }

  .zone-risks ul, .zone-features ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .zone-risks li, .zone-features li {
    padding: 0.35rem 0;
    font-size: 0.9rem;
    color: #475569;
  }

  /* Special Content */
  .special-content {
    border-left: 4px solid var(--mich-primary);
  }

  .special-content p {
    color: #475569;
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .special-stats, .aguacate-stats, .mariposa-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-top: 1rem;
  }

  .special-stats .stat, .aguacate-stats .stat, .mariposa-stats .stat {
    text-align: center;
    padding: 1rem;
    background: var(--mich-gradient);
    border-radius: 0.75rem;
    color: white;
  }

  .stat-icon { display: block; font-size: 1.5rem; margin-bottom: 0.25rem; }
  .stat-value { font-size: 0.85rem; font-weight: 600; }

  /* Morelia Special */
  .morelia-special { border-left-color: #7c3aed; }

  /* Pátzcuaro Special */
  .patzcuaro-special { border-left-color: #f59e0b; }

  .muertos-info {
    background: #fffbeb;
    padding: 1rem;
    border-radius: 0.75rem;
    margin-top: 1rem;
  }

  .muertos-badge {
    display: inline-block;
    background: #f59e0b;
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 1rem;
    font-size: 0.8rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .muertos-info ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .muertos-info li {
    padding: 0.35rem 0;
    font-size: 0.9rem;
    color: #92400e;
  }

  /* Uruapan Special */
  .aguacate-special { border-left-color: #16a34a; }

  /* Puerto Special */
  .puerto-special { border-left-color: #1e40af; }

  .puerto-capabilities {
    background: #eff6ff;
    padding: 1rem;
    border-radius: 0.75rem;
    margin-top: 1rem;
  }

  .puerto-capabilities h3 {
    color: #1e40af;
    font-size: 1rem;
    margin-bottom: 0.75rem;
  }

  .puerto-capabilities ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .puerto-capabilities li {
    padding: 0.35rem 0;
    font-size: 0.9rem;
    color: #1e3a8a;
  }

  /* Mariposa Special */
  .mariposa-special { border-left-color: #ea580c; }

  .mariposa-info {
    background: #fff7ed;
    padding: 1rem;
    border-radius: 0.75rem;
    margin-top: 1rem;
  }

  .unesco-badge {
    display: inline-block;
    background: linear-gradient(135deg, #ea580c, #f59e0b);
    color: white;
    padding: 0.35rem 0.75rem;
    border-radius: 1rem;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  /* Sidebar */
  .station-sidebar {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .sidebar-card {
    background: white;
    border-radius: 1rem;
    padding: 1.25rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06);
  }

  .sidebar-card h3 {
    font-size: 1rem;
    color: var(--mich-dark);
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid #e2e8f0;
  }

  /* Contact Card */
  .contact-phone {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem;
    border-radius: 0.75rem;
    text-decoration: none;
    margin-bottom: 0.75rem;
    transition: transform 0.2s;
  }

  .contact-phone:hover { transform: translateX(4px); }
  .contact-phone.main { background: var(--mich-gradient); color: white; }
  .contact-phone.secondary { background: #f1f5f9; color: #334155; }
  .contact-phone.emergency { background: linear-gradient(135deg, #dc2626, #f87171); color: white; }

  .phone-info { display: flex; flex-direction: column; }
  .phone-info strong { font-size: 1.1rem; }
  .phone-info small { font-size: 0.75rem; opacity: 0.85; }

  /* Location Card */
  .location-card address {
    font-style: normal;
    color: #475569;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .map-link {
    display: inline-block;
    color: var(--mich-primary);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9rem;
  }

  .map-link:hover { text-decoration: underline; }

  /* Hours Card */
  .hours-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .status-dot {
    width: 10px;
    height: 10px;
    background: #16a34a;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  .status-text { font-weight: 600; color: #16a34a; }

  .hours-detail {
    color: #64748b;
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }

  .response-info {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem;
    background: #fef3c7;
    border-radius: 0.5rem;
    font-size: 0.85rem;
    color: #92400e;
  }

  /* Command Card */
  .command-card p { color: #475569; font-weight: 500; }

  /* State Card */
  .state-card {
    border: 2px solid var(--mich-primary);
    background: linear-gradient(to bottom, white, #faf5ff);
  }

  .state-info {
    list-style: none;
    padding: 0;
    margin: 0 0 1rem 0;
  }

  .state-info li {
    padding: 0.4rem 0;
    font-size: 0.9rem;
    color: #475569;
    border-bottom: 1px solid #e2e8f0;
  }

  .state-info li:last-child { border-bottom: none; }

  .back-link {
    display: block;
    text-align: center;
    color: var(--mich-primary);
    text-decoration: none;
    font-weight: 600;
    padding: 0.75rem;
    background: white;
    border-radius: 0.5rem;
    border: 1px solid var(--mich-primary);
    transition: background 0.2s;
  }

  .back-link:hover {
    background: var(--mich-primary);
    color: white;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .special-stats, .aguacate-stats, .mariposa-stats {
      grid-template-columns: 1fr;
    }

    .hero-phones { flex-direction: column; }
  }
</style>
