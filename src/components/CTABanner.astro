---
/**
 * ─── CTA Banner Component ───
 * Banner de llamada a acción antes del footer
 * Diseño con gradiente y botón principal
 */
---

<section class="cta-banner">
  <div class="container">
    <div class="cta-banner__content">
      <div class="cta-banner__text">
        <h2 class="cta-banner__title">Explora Nuestro Catálogo Completo</h2>
        <p class="cta-banner__desc">Más de <strong>500 productos</strong> certificados NFPA listos para envío inmediato a toda la República</p>
      </div>
      <a href="/productos" class="cta-banner__btn">
        Ver Catálogo
      </a>
    </div>
  </div>
</section>

<style>
  .cta-banner {
    padding: var(--space-8) 0;
    background: linear-gradient(135deg, #dc2626 0%, #ea580c 50%, #f97316 100%);
  }

  .cta-banner__content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-10);
  }

  .cta-banner__text {
    text-align: left;
  }

  .cta-banner__title {
    font-size: var(--text-2xl);
    font-weight: 800;
    color: white;
    margin: 0;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
  }

  .cta-banner__desc {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.9);
    margin: var(--space-2) 0 0;
  }

  .cta-banner__desc strong {
    color: white;
    font-weight: 700;
  }

  .cta-banner__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-4) var(--space-8);
    background: white;
    color: #dc2626;
    font-size: var(--text-lg);
    font-weight: 700;
    text-decoration: none;
    border-radius: var(--radius-full);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
    transition: transform 0.2s, box-shadow 0.2s;
    flex-shrink: 0;
  }

  .cta-banner__btn:hover {
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
  }

  @media (max-width: 768px) {
    .cta-banner__content {
      flex-direction: column;
      text-align: center;
      gap: var(--space-6);
    }

    .cta-banner__text {
      text-align: center;
    }

    .cta-banner__title {
      font-size: var(--text-xl);
    }

    .cta-banner__btn {
      width: 100%;
    }
  }
</style>
