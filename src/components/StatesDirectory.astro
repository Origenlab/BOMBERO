---
/**
 * ─── States Directory Component ───
 * Directorio de estaciones de bomberos por estado
 * Grid de los 32 estados de la República Mexicana
 */
import SectionHeader from "./SectionHeader.astro";

interface State {
  name: string;
  slug: string;
  abbr: string;
}

const states: State[] = [
  { name: "Aguascalientes", slug: "aguascalientes", abbr: "AGS" },
  { name: "Baja California", slug: "baja-california", abbr: "BC" },
  { name: "Baja California Sur", slug: "baja-california-sur", abbr: "BCS" },
  { name: "Campeche", slug: "campeche", abbr: "CAM" },
  { name: "Chiapas", slug: "chiapas", abbr: "CHIS" },
  { name: "Chihuahua", slug: "chihuahua", abbr: "CHIH" },
  { name: "Ciudad de México", slug: "ciudad-de-mexico", abbr: "CDMX" },
  { name: "Coahuila", slug: "coahuila", abbr: "COAH" },
  { name: "Colima", slug: "colima", abbr: "COL" },
  { name: "Durango", slug: "durango", abbr: "DGO" },
  { name: "Estado de México", slug: "estado-de-mexico", abbr: "EDOMEX" },
  { name: "Guanajuato", slug: "guanajuato", abbr: "GTO" },
  { name: "Guerrero", slug: "guerrero", abbr: "GRO" },
  { name: "Hidalgo", slug: "hidalgo", abbr: "HGO" },
  { name: "Jalisco", slug: "jalisco", abbr: "JAL" },
  { name: "Michoacán", slug: "michoacan", abbr: "MICH" },
  { name: "Morelos", slug: "morelos", abbr: "MOR" },
  { name: "Nayarit", slug: "nayarit", abbr: "NAY" },
  { name: "Nuevo León", slug: "nuevo-leon", abbr: "NL" },
  { name: "Oaxaca", slug: "oaxaca", abbr: "OAX" },
  { name: "Puebla", slug: "puebla", abbr: "PUE" },
  { name: "Querétaro", slug: "queretaro", abbr: "QRO" },
  { name: "Quintana Roo", slug: "quintana-roo", abbr: "QROO" },
  { name: "San Luis Potosí", slug: "san-luis-potosi", abbr: "SLP" },
  { name: "Sinaloa", slug: "sinaloa", abbr: "SIN" },
  { name: "Sonora", slug: "sonora", abbr: "SON" },
  { name: "Tabasco", slug: "tabasco", abbr: "TAB" },
  { name: "Tamaulipas", slug: "tamaulipas", abbr: "TAMPS" },
  { name: "Tlaxcala", slug: "tlaxcala", abbr: "TLAX" },
  { name: "Veracruz", slug: "veracruz", abbr: "VER" },
  { name: "Yucatán", slug: "yucatan", abbr: "YUC" },
  { name: "Zacatecas", slug: "zacatecas", abbr: "ZAC" },
];
---

<section class="states-dir" id="directorio">
  <div class="container">
    <SectionHeader
      label="Directorio Nacional"
      title="Estaciones de Bomberos en México"
      description="Encuentra la estación de bomberos más cercana a ti. Directorio completo con información de contacto, teléfonos de emergencia y ubicación de todas las estaciones en los 32 estados."
    >
      <p>
        En <strong>BOMBERO.MX</strong> hemos desarrollado el <strong>directorio más completo de estaciones de bomberos en México</strong>.
        Nuestra base de datos incluye información actualizada de los <strong>H. Cuerpos de Bomberos</strong> en las 32 entidades federativas:
        números de emergencia, direcciones físicas, horarios de atención y datos de contacto directo. Ya sea que te encuentres en
        <strong>Ciudad de México, Monterrey, Guadalajara, Tijuana, Puebla, León</strong> o cualquier municipio del país,
        aquí encontrarás la estación de bomberos más cercana a tu ubicación.
      </p>
      <p>
        Este directorio es un <strong>servicio gratuito para la comunidad</strong> que busca facilitar el acceso a los servicios de emergencia
        y reconocer la labor heroica de los bomberos mexicanos. Además de los <strong>teléfonos de emergencia locales</strong>,
        incluimos información sobre servicios especializados: rescate vehicular, atención a incendios estructurales,
        materiales peligrosos (HAZMAT), rescate en alturas y servicios pre-hospitalarios.
        <strong>Conoce a tu cuerpo de bomberos local</strong> y mantén sus datos de contacto siempre a la mano.
      </p>
    </SectionHeader>

    <div class="states-dir__grid">
      {states.map((state) => (
        <a href={`/directorio/${state.slug}`} class="states-dir__card">
          <span class="states-dir__abbr">{state.abbr}</span>
          <span class="states-dir__name">{state.name}</span>
          <span class="states-dir__arrow"></span>
        </a>
      ))}
    </div>

    <div class="states-dir__footer">
      <div class="states-dir__stats">
        <div class="states-dir__stat">
          <span class="states-dir__stat-number">32</span>
          <span class="states-dir__stat-label">Estados</span>
        </div>
        <div class="states-dir__stat">
          <span class="states-dir__stat-number">602+</span>
          <span class="states-dir__stat-label">Estaciones</span>
        </div>
        <div class="states-dir__stat">
          <span class="states-dir__stat-number">24/7</span>
          <span class="states-dir__stat-label">Emergencias</span>
        </div>
      </div>
      <a href="/directorio" class="states-dir__btn">
        Ver Directorio Nacional Completo
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  /* ═══════════════════════════════════════════════════════════════════════
     STATES DIRECTORY - Directorio de Estaciones por Estado
     ═══════════════════════════════════════════════════════════════════════ */

  .states-dir {
    padding: var(--space-20) 0;
    background: var(--color-smoke-50);
  }

  /* ═══ Grid de Estados ═══ */
  .states-dir__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-4);
  }

  /* ═══ Card de Estado ═══ */
  .states-dir__card {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-5);
    background: white;
    border-radius: var(--radius-xl);
    text-decoration: none;
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
  }

  .states-dir__card::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: var(--color-fire-500);
    transform: scaleY(0);
    transition: transform 0.2s ease;
  }

  .states-dir__card:hover {
    background: white;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    transform: translateY(-2px);
  }

  .states-dir__card:hover::before {
    transform: scaleY(1);
  }

  .states-dir__abbr {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    min-width: 48px;
    background: var(--color-smoke-100);
    border-radius: var(--radius-lg);
    font-size: var(--text-xs);
    font-weight: 800;
    color: var(--color-smoke-600);
    letter-spacing: 0.5px;
    transition: all 0.2s ease;
  }

  .states-dir__card:hover .states-dir__abbr {
    background: linear-gradient(135deg, var(--color-fire-500) 0%, var(--color-ember-500) 100%);
    color: white;
  }

  .states-dir__name {
    flex: 1;
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-smoke-800);
    line-height: 1.3;
  }

  .states-dir__card:hover .states-dir__name {
    color: var(--color-smoke-900);
  }

  .states-dir__arrow {
    width: 20px;
    height: 20px;
    min-width: 20px;
    position: relative;
    opacity: 0;
    transform: translateX(-8px);
    transition: all 0.2s ease;
  }

  .states-dir__arrow::before,
  .states-dir__arrow::after {
    content: '';
    position: absolute;
    background: var(--color-fire-500);
  }

  .states-dir__arrow::before {
    width: 12px;
    height: 2px;
    top: 50%;
    left: 2px;
    transform: translateY(-50%);
  }

  .states-dir__arrow::after {
    width: 8px;
    height: 8px;
    right: 2px;
    top: 50%;
    transform: translateY(-50%) rotate(45deg);
    background: transparent;
    border-top: 2px solid var(--color-fire-500);
    border-right: 2px solid var(--color-fire-500);
  }

  .states-dir__card:hover .states-dir__arrow {
    opacity: 1;
    transform: translateX(0);
  }

  /* ═══ Footer con Stats ═══ */
  .states-dir__footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: var(--space-10);
    padding: var(--space-8);
    background: linear-gradient(135deg, var(--color-smoke-900) 0%, var(--color-smoke-800) 100%);
    border-radius: var(--radius-2xl);
  }

  .states-dir__stats {
    display: flex;
    gap: var(--space-10);
  }

  .states-dir__stat {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .states-dir__stat-number {
    font-size: var(--text-3xl);
    font-weight: 800;
    color: white;
    line-height: 1;
  }

  .states-dir__stat-label {
    font-size: var(--text-sm);
    color: var(--color-smoke-400);
    font-weight: 500;
  }

  .states-dir__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-8);
    background: linear-gradient(135deg, var(--color-fire-500) 0%, var(--color-ember-500) 100%);
    color: white;
    font-size: var(--text-base);
    font-weight: 700;
    text-decoration: none;
    border-radius: var(--radius-full);
    transition: all 0.2s ease;
  }

  .states-dir__btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(220, 38, 38, 0.4);
  }

  .states-dir__btn svg {
    transition: transform 0.2s ease;
  }

  .states-dir__btn:hover svg {
    transform: translateX(4px);
  }

  /* ═══ Responsive ═══ */
  @media (max-width: 1200px) {
    .states-dir__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 1024px) {
    .states-dir__footer {
      flex-direction: column;
      gap: var(--space-6);
      text-align: center;
    }

    .states-dir__stats {
      justify-content: center;
    }
  }

  @media (max-width: 768px) {
    .states-dir {
      padding: var(--space-16) 0;
    }

    .states-dir__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-3);
    }

    .states-dir__card {
      padding: var(--space-3) var(--space-4);
    }

    .states-dir__abbr {
      width: 40px;
      height: 40px;
      min-width: 40px;
      font-size: 10px;
    }

    .states-dir__name {
      font-size: var(--text-xs);
    }

    .states-dir__arrow {
      display: none;
    }

    .states-dir__footer {
      padding: var(--space-6);
    }

    .states-dir__stats {
      gap: var(--space-6);
    }

    .states-dir__stat-number {
      font-size: var(--text-2xl);
    }

    .states-dir__btn {
      width: 100%;
    }
  }

  @media (max-width: 480px) {
    .states-dir__grid {
      grid-template-columns: 1fr;
    }

    .states-dir__card {
      padding: var(--space-4);
    }

    .states-dir__abbr {
      width: 44px;
      height: 44px;
      min-width: 44px;
    }

    .states-dir__name {
      font-size: var(--text-sm);
    }
  }
</style>
