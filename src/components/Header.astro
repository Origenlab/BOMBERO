---

import TopBar from "./TopBar.astro";
import Nav from "./Nav.astro";
import Logo from "./Logo.astro";
import { SITE } from "@data/site";
import { productCategories, secondaryCategories } from "@data/navigation";

interface Props {
  variant?: "default" | "transparent" | "dark";
  sticky?: boolean;
}

const { variant = "default", sticky = true } = Astro.props;

const headerClasses = [
  "header",
  `header--${variant}`,
  sticky && "header--sticky",
].filter(Boolean).join(" ");

// Iconos para menú móvil — set completo para las 16 categorías
const icons: Record<string, string> = {
  shield:      `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>`,
  helmet:      `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><line x1="9" y1="9" x2="9.01" y2="9"/><line x1="15" y1="9" x2="15.01" y2="9"/></svg>`,
  gloves:      `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M18 11V6a2 2 0 0 0-2-2a2 2 0 0 0-2 2"/><path d="M14 10V4a2 2 0 0 0-2-2a2 2 0 0 0-2 2v2"/><path d="M10 10.5V6a2 2 0 0 0-2-2a2 2 0 0 0-2 2v8"/><path d="M18 8a2 2 0 1 1 4 0v6a8 8 0 0 1-8 8h-2c-2.8 0-4.5-.86-5.99-2.34l-3.6-3.6a2 2 0 0 1 2.83-2.82L7 15"/></svg>`,
  boot:        `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 21h18"/><path d="M19 21v-4H5v4"/><path d="M19 17V9a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v8"/><path d="M7 7V4a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v3"/></svg>`,
  hood:        `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2a8 8 0 0 0-8 8v4a8 8 0 0 0 16 0v-4a8 8 0 0 0-8-8z"/><circle cx="12" cy="10" r="3"/></svg>`,
  goggles:     `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="7" cy="12" r="3"/><circle cx="17" cy="12" r="3"/><path d="M10 12h4"/><path d="M4 12H2"/><path d="M22 12h-2"/></svg>`,
  lungs:       `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M6.081 20C2.5 20 2 16.5 2 14c0-3 1-6.5 4-7.5 1-.33 1.5-1 1.5-2 0-.17.01-.33.03-.5"/><path d="M17.92 20C21.5 20 22 16.5 22 14c0-3-1-6.5-4-7.5-1-.33-1.5-1-1.5-2 0-.17-.01-.33-.03-.5"/><path d="M12 4v16"/><path d="M8 4h8"/></svg>`,
  fire:        `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/></svg>`,
  tools:       `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>`,
  camera:      `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z"/><circle cx="12" cy="13" r="3"/></svg>`,
  extinguisher:`<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M10 2v2"/><path d="M14 2v2"/><rect x="7" y="4" width="10" height="16" rx="2"/><path d="M12 4v4"/><path d="M7 12h10"/></svg>`,
  forest:      `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2L4 12h4v8h8v-8h4L12 2z"/></svg>`,
  hazmat:      `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>`,
  rope:        `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2v20"/><path d="M8 6c0 0 2-2 4-2s4 2 4 2"/><path d="M8 10c0 0 2-2 4-2s4 2 4 2"/><path d="M8 14c0 0 2-2 4-2s4 2 4 2"/><circle cx="12" cy="20" r="2"/></svg>`,
  water:       `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z"/></svg>`,
  medical:     `<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M12 8v8"/><path d="M8 12h8"/></svg>`,
};

// Pre-encoded WA message — encodeURIComponent en build time para evitar
// que acentos/espacios lleguen crudos a la URL (ej: "ó" → "%C3%B3").
const waTextDesktop = encodeURIComponent("Hola, me interesa información sobre equipo para bomberos");
const waTextMobile  = encodeURIComponent("Hola, necesito cotizar equipo para bomberos certificado NFPA. ¿Pueden asesorarme?");
---


<TopBar />


<header class={headerClasses} role="banner">
  <div class="header__bar full-bleed">
    <div class="container container--wide header__inner">

    <Logo />


    <nav class="header__nav" id="main-nav" aria-label="Navegación principal">
      <Nav />
    </nav>


    <div class="header__actions">

      <a
        href={`https://wa.me/${SITE.contact.whatsapp}?text=${waTextDesktop}`}
        class="header__whatsapp"
        aria-label="Contactar por WhatsApp"
        target="_blank"
        rel="noopener noreferrer"
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
          <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
        </svg>
      </a>


      <a href="/cotizar/" class="header__cta">
        <span>Cotizar</span>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>


      <button
        class="header__toggle"
        aria-expanded="false"
        aria-controls="mobile-menu"
        aria-label="Abrir menú de navegación"
        type="button"
      >
        <span class="header__toggle-bar" aria-hidden="true"></span>
        <span class="header__toggle-bar" aria-hidden="true"></span>
        <span class="header__toggle-bar" aria-hidden="true"></span>
      </button>
    </div>
    </div>
  </div>


  <div class="mobile-menu__overlay" aria-hidden="true"></div>

  <div class="mobile-menu" id="mobile-menu" role="dialog" aria-modal="true" aria-label="Menú de navegación" aria-hidden="true">

    <!-- Cabecera del drawer -->
    <div class="mobile-menu__brand">
      <a href="/" class="mobile-menu__brand-logo">BOMBERO<span>.MX</span></a>
      <span class="mobile-menu__brand-tag">Equipo NFPA certificado</span>
    </div>

    <div class="mobile-menu__inner">

      <nav class="mobile-menu__nav" aria-label="Navegación móvil">
        <a href="/" class="mobile-menu__link">
          <span>Inicio</span>
          <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
        </a>

        <div class="mobile-menu__accordion">
          <button class="mobile-menu__accordion-btn" type="button" aria-expanded="false">
            <span class="mobile-menu__accordion-label">
              <svg aria-hidden="true" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
              Productos
              <span class="mobile-menu__accordion-count">16 categorías</span>
            </span>
            <svg aria-hidden="true" class="mobile-menu__accordion-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M6 9l6 6 6-6"/>
            </svg>
          </button>

          <div class="mobile-menu__accordion-content">

            <!-- Sección 1: EPP -->
            <p class="mobile-menu__section-label">
              <span class="mobile-menu__section-dot"></span>
              Equipo de Protección Personal
            </p>
            <div class="mobile-menu__categories">
              {productCategories.map((cat) => (
                <a href={cat.href} class="mobile-menu__category">
                  <span class="mobile-menu__category-icon" set:html={icons[cat.icon ?? "shield"]} />
                  <span class="mobile-menu__category-name">{cat.label}</span>
                </a>
              ))}
            </div>

            <!-- Sección 2: Especializados -->
            <p class="mobile-menu__section-label mobile-menu__section-label--2">
              <span class="mobile-menu__section-dot mobile-menu__section-dot--blue"></span>
              Equipos Especializados
            </p>
            <div class="mobile-menu__categories">
              {secondaryCategories.map((cat) => (
                <a href={cat.href} class="mobile-menu__category">
                  <span class="mobile-menu__category-icon mobile-menu__category-icon--blue" set:html={icons[cat.icon ?? "tools"]} />
                  <span class="mobile-menu__category-name">{cat.label}</span>
                </a>
              ))}
            </div>

            <!-- CTA catálogo -->
            <a href="/productos/" class="mobile-menu__all-products">
              <svg aria-hidden="true" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
              Ver catálogo completo
              <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>

          </div>
        </div>

        <a href="/directorio/" class="mobile-menu__link">
          <span>Directorio</span>
          <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
        </a>
        <a href="/blog/" class="mobile-menu__link">
          <span>Blog</span>
          <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
        </a>
        <a href="/nosotros/" class="mobile-menu__link">
          <span>Nosotros</span>
          <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
        </a>
        <a href="/contacto/" class="mobile-menu__link">
          <span>Contacto</span>
          <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
        </a>
      </nav>

      <!-- CTAs principales -->
      <div class="mobile-menu__cta">
        <a href="/cotizar/" class="mobile-menu__cta-btn mobile-menu__cta-btn--primary">
          <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14,2 14,8 20,8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
          Solicitar Cotización
        </a>
        <a
          href={`https://wa.me/${SITE.contact.whatsapp}?text=${waTextMobile}`}
          class="mobile-menu__cta-btn mobile-menu__cta-btn--wa"
          target="_blank"
          rel="noopener noreferrer"
        >
          <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
          </svg>
          WhatsApp Directo
        </a>
      </div>

      <!-- Contacto rápido -->
      <div class="mobile-menu__contact">
        <a href={`tel:${SITE.contact.phoneRaw}`} class="mobile-menu__contact-item">
          <span class="mobile-menu__contact-icon">
            <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
          </span>
          <span>{SITE.contact.phoneTollFree}</span>
        </a>
        <a href={`mailto:${SITE.contact.email}`} class="mobile-menu__contact-item">
          <span class="mobile-menu__contact-icon">
            <svg aria-hidden="true" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
          </span>
          <span>{SITE.contact.email}</span>
        </a>
      </div>

    </div>
  </div>
</header>

<script>
  // Módulo diferido por Astro — DOM ya disponible, sin wrapper DOMContentLoaded
  var toggle = document.querySelector(".header__toggle");
  var mobileMenu = document.getElementById("mobile-menu");
  var overlay = document.querySelector(".mobile-menu__overlay");
  var header = document.querySelector(".header");

  // Bloqueo de scroll robusto: html + body + clase en body (para ocultar floats).
  function setScrollLock(locked) {
    document.documentElement.style.overflow = locked ? "hidden" : "";
    document.body.style.overflow = locked ? "hidden" : "";
    document.body.classList.toggle("menu-open", locked);
  }

  function isOpen() {
    return !!(mobileMenu && mobileMenu.classList.contains("mobile-menu--open"));
  }

  // Focus trap: mantiene el foco dentro del drawer mientras está abierto (a11y)
  function trapFocus(e) {
    if (e.key !== "Tab" || !isOpen() || !mobileMenu) return;
    var f = mobileMenu.querySelectorAll('a[href],button:not([disabled])');
    if (!f.length) return;
    var first = f[0], last = f[f.length - 1];
    if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
    else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
  }

  function openMenu() {
    if (!mobileMenu) return;
    if (toggle) toggle.setAttribute("aria-expanded", "true");
    mobileMenu.setAttribute("aria-hidden", "false");
    mobileMenu.classList.add("mobile-menu--open");
    if (overlay) overlay.classList.add("is-active");
    if (header) header.classList.add("header--menu-open");
    setScrollLock(true);
    // Enfocar el primer enlace para lectores de pantalla / teclado
    var firstLink = mobileMenu.querySelector("a[href],button");
    if (firstLink) { try { firstLink.focus({ preventScroll: true }); } catch (e) { firstLink.focus(); } }
  }

  function closeMenu() {
    if (toggle) toggle.setAttribute("aria-expanded", "false");
    if (mobileMenu) {
      mobileMenu.setAttribute("aria-hidden", "true");
      mobileMenu.classList.remove("mobile-menu--open");
    }
    if (overlay) overlay.classList.remove("is-active");
    if (header) header.classList.remove("header--menu-open");
    setScrollLock(false);
  }

  if (overlay) overlay.addEventListener("click", closeMenu);
  document.addEventListener("keydown", trapFocus);

  if (toggle) {
    toggle.addEventListener("click", function() {
      isOpen() ? closeMenu() : openMenu();
    });
  }

  // Accordion functionality
  var accordionBtns = document.querySelectorAll(".mobile-menu__accordion-btn");
  accordionBtns.forEach(function(btn) {
    btn.addEventListener("click", function() {
      var isExpanded = btn.getAttribute("aria-expanded") === "true";
      btn.setAttribute("aria-expanded", String(!isExpanded));
      var accordion = btn.closest(".mobile-menu__accordion");
      if (accordion) accordion.classList.toggle("is-open");
    });
  });

  // Cerrar el menú al tocar cualquier enlace de navegación dentro de él
  if (mobileMenu) {
    mobileMenu.querySelectorAll("a[href]").forEach(function(a) {
      a.addEventListener("click", function() { closeMenu(); });
    });
  }

  // Cerrar al hacer clic fuera del header
  document.addEventListener("click", function(e) {
    var target = e.target;
    if (isOpen() && target && target.closest && !target.closest(".header")) closeMenu();
  });

  // Cerrar con Escape
  document.addEventListener("keydown", function(e) {
    if (e.key === "Escape" && isOpen()) closeMenu();
  });

  // CRÍTICO: resetear estado obsoleto al cargar y al restaurar desde bfcache.
  // En iOS Safari, al volver atrás la página se restaura con body.overflow=hidden
  // pegado y el menú "abierto" -> el usuario no puede hacer scroll. pageshow
  // (incluido el restore persistido) fuerza el cierre y libera el scroll.
  window.addEventListener("pageshow", function() { closeMenu(); });

  // Si se redimensiona a ancho desktop con el menú abierto, restaurar scroll
  window.addEventListener("resize", function() {
    if (window.innerWidth > 768 && isOpen()) closeMenu();
  });

  // Header scroll effect — throttled con rAF + passive
  var ticking = false;
  var isScrolled = false;
  function onScroll() {
    if (ticking) return;
    ticking = true;
    window.requestAnimationFrame(function() {
      var shouldScroll = window.pageYOffset > 100;
      if (shouldScroll !== isScrolled && header) {
        header.classList.toggle("header--scrolled", shouldScroll);
        isScrolled = shouldScroll;
      }
      ticking = false;
    });
  }
  window.addEventListener("scroll", onScroll, { passive: true });
</script>

<style>


  /* ── Header limpio con fondo blanco ── */
  .header {
    position: relative;
    width: 100%;
    background: #ffffff;
    border-bottom: 1px solid var(--color-smoke-100);
    color: var(--color-smoke-800);
  }

  .header__bar {
    position: relative;
    padding: var(--space-3) 0;
  }

  /* Línea acento roja visible al hacer scroll */
  .header::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -1px;
    height: 2px;
    background: linear-gradient(90deg, transparent 0%, var(--color-ember-600) 25%, var(--color-ember-600) 75%, transparent 100%);
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .header--scrolled::after {
    opacity: 0.7;
  }

  .header--sticky {
    position: sticky;
    top: 0;
    z-index: var(--z-header);
  }

  .header--scrolled {
    box-shadow: 0 4px 20px -8px rgba(15, 23, 42, 0.12);
    border-bottom-color: transparent;
    background: rgba(255, 255, 255, 0.96);
    backdrop-filter: blur(16px) saturate(180%);
    -webkit-backdrop-filter: blur(16px) saturate(180%);
  }

  .header--transparent {
    background: transparent;
    position: absolute;
    width: 100%;
    left: 0;
    top: 0;
    border-bottom-color: transparent;
  }

  .header--dark {
    background: var(--surface-0);
  }

  .header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-8);
  }


  .header__nav {
    flex: 1;
    display: flex;
    justify-content: center;
  }


  .header__actions {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .header__whatsapp {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: #25D366;
    border-radius: var(--radius-lg);
    color: white;
    transition: all 0.15s ease;
  }

  .header__whatsapp:hover {
    background: #128C7E;
    transform: scale(1.05);
  }

  .header__cta {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: 0.625rem 1.125rem;
    background: var(--gradient-cta);
    color: white;
    font-weight: 700;
    font-size: var(--text-sm);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-md);
    transition: all 0.2s ease;
    box-shadow: 0 4px 16px -4px rgba(220, 38, 38, 0.5), inset 0 1px 0 rgba(255,255,255,0.15);
    position: relative;
    overflow: hidden;
  }

  .header__cta::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.2) 0%, transparent 50%);
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  .header__cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 8px 24px -6px rgba(220, 38, 38, 0.6), inset 0 1px 0 rgba(255,255,255,0.2);
  }

  .header__cta:hover::before {
    opacity: 1;
  }

  .header__cta svg {
    transition: transform 0.2s ease;
  }

  .header__cta:hover svg {
    transform: translateX(2px);
  }


  .header__toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    background: none;
    border: none;
    cursor: pointer;
    padding: var(--space-2);
    border-radius: var(--radius-lg);
    transition: background 0.15s ease;
  }

  .header__toggle:hover {
    background: var(--color-smoke-100);
  }

  .header__toggle-bar {
    display: block;
    width: 22px;
    height: 2px;
    background: var(--color-smoke-700);
    border-radius: 2px;
    transition: all 0.2s ease;
    transform-origin: center;
  }

  .header__toggle[aria-expanded="true"] .header__toggle-bar:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .header__toggle[aria-expanded="true"] .header__toggle-bar:nth-child(2) {
    opacity: 0;
    transform: scaleX(0);
  }

  .header__toggle[aria-expanded="true"] .header__toggle-bar:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }



  /* ── Overlay oscuro detrás del drawer ── */
  .mobile-menu__overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(15, 23, 42, 0.55);
    -webkit-backdrop-filter: blur(2px);
    backdrop-filter: blur(2px);
    opacity: 0;
    visibility: hidden;
    /* CRÍTICO: sin esto el overlay (inset:0, z 98) puede capturar toques
       sobre el WhatsApp/back-to-top aunque esté invisible. */
    pointer-events: none;
    transition: opacity 0.3s var(--ease-out), visibility 0.3s;
    z-index: calc(var(--z-header) - 2);
  }

  .mobile-menu__overlay.is-active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
  }

  /* ── Drawer lateral (entra desde la derecha) ── */
  .mobile-menu {
    display: none;
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    left: auto;
    width: min(86vw, 380px);
    background: #ffffff;
    z-index: calc(var(--z-header) - 1);
    padding-top: 0;
    transform: translateX(100%);
    visibility: hidden;
    transition: transform 0.32s cubic-bezier(0.4, 0, 0.2, 1), visibility 0.32s;
    box-shadow: -8px 0 40px -10px rgba(15, 23, 42, 0.35);
    overscroll-behavior: contain;
    will-change: transform;
  }

  .mobile-menu--open {
    transform: translateX(0);
    visibility: visible;
  }

  .mobile-menu__inner {
    display: flex;
    flex-direction: column;
    height: 100%;
    padding: var(--space-4) var(--space-5);
    padding-bottom: calc(var(--space-6) + var(--safe-bottom, 0px));
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
    gap: var(--space-5);
  }

  @media (prefers-reduced-motion: reduce) {
    .mobile-menu { transition: none; }
    .mobile-menu__overlay { transition: none; }
  }


  .mobile-menu__nav {
    display: flex;
    flex-direction: column;
  }

  .mobile-menu__link {
    display: flex;
    align-items: center;
    min-height: var(--touch-target-min);
    padding: var(--space-4) 0;
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-smoke-800);
    text-decoration: none;
    border-bottom: 1px solid var(--color-smoke-100);
    transition: color 0.15s ease;
    touch-action: manipulation;
  }

  .mobile-menu__link:active {
    color: var(--color-fire-600);
  }


  .mobile-menu__accordion {
    border-bottom: 1px solid var(--color-smoke-100);
  }

  .mobile-menu__accordion-btn {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: var(--space-4) 0;
    background: none;
    border: none;
    cursor: pointer;
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-smoke-800);
  }

  .mobile-menu__accordion-btn svg {
    transition: transform 0.2s ease;
    color: var(--color-smoke-400);
  }

  .mobile-menu__accordion.is-open .mobile-menu__accordion-btn svg {
    transform: rotate(180deg);
  }

  .mobile-menu__accordion-content {
    display: none;
    padding-bottom: var(--space-4);
  }

  .mobile-menu__accordion.is-open .mobile-menu__accordion-content {
    display: block;
  }

  .mobile-menu__categories {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-2);
  }

  .mobile-menu__category {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    min-height: var(--touch-target-min);
    padding: var(--space-3);
    background: var(--color-smoke-50);
    border-radius: var(--radius-lg);
    text-decoration: none;
    transition: background 0.15s ease;
    touch-action: manipulation;
  }

  .mobile-menu__category:active {
    background: var(--color-fire-50);
  }

  .mobile-menu__category-icon {
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-fire-600);
  }

  .mobile-menu__category-icon :global(svg) {
    width: 18px;
    height: 18px;
  }

  .mobile-menu__category-name {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-smoke-800);
    line-height: 1.2;
  }

  .mobile-menu__all-products {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    margin-top: var(--space-3);
    padding: var(--space-3);
    background: var(--color-fire-50);
    border-radius: var(--radius-lg);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-fire-600);
    text-decoration: none;
  }

  .mobile-menu__all-products:hover {
    background: var(--color-fire-100);
  }


  .mobile-menu__cta {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .mobile-menu__cta-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    min-height: 52px;
    padding: var(--space-4);
    border-radius: var(--radius-xl);
    font-size: var(--text-base);
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.15s ease, box-shadow 0.2s ease;
    touch-action: manipulation;
  }

  .mobile-menu__cta-btn--primary {
    background: var(--gradient-fire);
    color: #ffffff;
    box-shadow: 0 2px 8px rgba(220, 38, 38, 0.25);
  }

  .mobile-menu__cta-btn--primary:active {
    transform: scale(0.98);
    box-shadow: 0 1px 4px rgba(220, 38, 38, 0.2);
  }

  .mobile-menu__cta-btn--secondary {
    background: #25D366;
    color: #ffffff;
  }

  .mobile-menu__cta-btn--secondary:active {
    transform: scale(0.98);
    background: #128C7E;
  }


  .mobile-menu__contact {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-smoke-100);
    margin-top: auto;
  }

  .mobile-menu__contact-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    min-height: var(--touch-target-min);
    padding: var(--space-2) 0;
    color: var(--color-smoke-600);
    text-decoration: none;
    font-size: var(--text-sm);
    touch-action: manipulation;
  }

  .mobile-menu__contact-item:active {
    color: var(--color-fire-600);
  }

  .mobile-menu__contact-item svg {
    flex-shrink: 0;
    color: var(--color-smoke-400);
  }



  @media (max-width: 1024px) {
    .header__cta span {
      display: none;
    }

    .header__cta {
      width: 40px;
      height: 40px;
      padding: 0;
      justify-content: center;
    }

    .header__cta svg {
      margin: 0;
    }
  }

  @media (max-width: 768px) {
    .header__nav {
      display: none;
    }

    /* Header compacto en móvil: gap menor y acciones que NO se encogen,
       para que el CTA y la hamburguesa siempre tengan su tamaño completo. */
    .header__inner {
      gap: var(--space-3);
    }

    .header__bar {
      padding: var(--space-2) 0;
    }

    .header__actions {
      flex-shrink: 0;
      gap: var(--space-2);
    }

    .header__cta {
      flex: 0 0 auto;
    }

    .header__toggle {
      display: flex;
      flex: 0 0 44px;
      width: 44px;
      height: 44px;
    }

    .mobile-menu,
    .mobile-menu__overlay {
      display: block;
    }

    .header__whatsapp {
      display: none;
    }

    .header--menu-open {
      position: fixed;
      width: 100%;
    }

    /* El .mobile-menu es hijo del <header> y comparte su contexto de apilado;
       con z-index 99 tapaba la barra (logo + botón X). Elevamos la barra por
       encima del panel para que el usuario SIEMPRE vea y pueda tocar la X. */
    .header--menu-open .header__bar {
      position: relative;
      z-index: 100;
    }
  }
</style>
